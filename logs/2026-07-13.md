# 2026-07-13 학습 기록

## 📌 토픽

상속과 다형성

## ❓ 질문 (3문제)

### 1. 🔧 문제 상황

**상황:**
당신은 배달 앱을 만들고 있습니다. 음식점은 "주문 준비 중" → "조리 완료" → "픽업 완료"의 상태를 가지고, 배달원은 "픽업 대기" → "배달 중" → "배달 완료"의 상태를 가집니다. 

둘 다 상태 변화에 따라 고객에게 알림을 보내야 하는데, 상태가 바뀔 때마다 비슷한 코드를 반복해서 작성하고 있습니다. 마찬가지로 앞으로 라이더, 편의점 등 새로운 배송 주체들이 계속 추가될 예정입니다.

**질문:**
이런 상황에서 "상속과 다형성"이 어떻게 도움이 될까요? 코드 반복을 줄이면서도 새로운 배송 주체를 쉽게 추가하려면 어떤 방식으로 생각해야 할까요?

## 💬 나의 답변

(답변 없음)

## 🤖 AI 피드백

(피드백 없음)

## 📚 모범 답안 & 개념 정리

### 1. 🔧 문제 상황

# 상속과 다형성을 활용한 배달앱 설계

## 모범 답안

이 문제는 **상속으로 공통 로직을 추상화**하고, **다형성으로 상태별 동작을 유연하게 처리**하는 전형적인 사례입니다.

### 1단계: 공통 추상화 (상속의 핵심)

```python
from abc import ABC, abstractmethod
from enum import Enum
from datetime import datetime

# 추상 기본 클래스: 모든 배송 주체의 공통 인터페이스 정의
class DeliveryEntity(ABC):
    def __init__(self, entity_id: str):
        self.entity_id = entity_id
        self.state = None
        self.state_history = []
    
    # 템플릿 메서드: 상태 변화의 일반적인 프로세스 정의
    def change_state(self, new_state):
        """상태 변경의 공통 로직 (중복 제거)"""
        if self._is_valid_transition(self.state, new_state):
            old_state = self.state
            self.state = new_state
            self.state_history.append({
                'from': old_state,
                'to': new_state,
                'timestamp': datetime.now()
            })
            
            # 다형성: 각 주체별 맞춤 동작
            self._on_state_changed(old_state, new_state)
            self._send_notification()  # 공통: 항상 실행
            self._update_related_data()  # 공통: 항상 실행
        else:
            raise InvalidStateTransition(f"{old_state} → {new_state}")
    
    # 추상 메서드: 구현체에서 반드시 정의해야 함
    @abstractmethod
    def _is_valid_transition(self, current: str, next: str) -> bool:
        """각 주체별 유효한 상태 전이만 허용"""
        pass
    
    @abstractmethod
    def _on_state_changed(self, old_state: str, new_state: str):
        """상태 변경 시 주체별 추가 로직"""
        pass
    
    @abstractmethod
    def get_notification_message(self) -> str:
        """주체별 맞춤 알림 메시지"""
        pass
    
    # 공통 메서드: 모든 구현체에서 동일
    def _send_notification(self):
        """공통 알림 로직"""
        message = self.get_notification_message()
        NotificationService.send(self.entity_id, message)
    
    def _update_related_data(self):
        """상태 변경 시 관련 데이터 업데이트"""
        OrderStatusCache.update(self.entity_id, self.state)
```

### 2단계: 구체적 구현 (상속을 통한 확장)

```python
# 음식점 클래스
class Restaurant(DeliveryEntity):
    def __init__(self, restaurant_id: str):
        super().__init__(restaurant_id)
        self.state = "preparing"  # 초기 상태
    
    def _is_valid_transition(self, current: str, next: str) -> bool:
        """음식점의 유효한 상태 전이"""
        valid_transitions = {
            "preparing": ["completed"],
            "completed": ["pickup_completed"],
            "pickup_completed": []
        }
        return next in valid_transitions.get(current, [])
    
    def _on_state_changed(self, old_state: str, new_state: str):
        """음식점 특화 로직"""
        if new_state == "completed":
            # 라이더에게 픽업 준비 완료 알림
            self._notify_available_riders()
        elif new_state == "pickup_completed":
            # 주방 통계 업데이트
            KitchenStats.update_completion_time(self.entity_id)
    
    def get_notification_message(self) -> str:
        messages = {
            "preparing": "주문이 준비 중입니다",
            "completed": "조리가 완료되었습니다. 픽업 대기 중입니다",
            "pickup_completed": "주문이 픽업되었습니다"
        }
        return messages.get(self.state, "상태 변경됨")
    
    def _notify_available_riders(self):
        """픽업 가능한 라이더들에게 알림"""
        available_riders = RiderPool.get_nearby_riders(self.entity_id)
        for rider in available_riders:
            rider.notify_pickup_available(self.entity_id)


# 배달원 클래스
class DeliveryRider(DeliveryEntity):
    def __init__(self, rider_id: str):
        super().__init__(rider_id)
        self.state = "waiting_pickup"
        self.current_order_id = None
    
    def _is_valid_transition(self, current: str, next: str) -> bool:
        """배달원의 유효한 상태 전이"""
        valid_transitions = {
            "waiting_pickup": ["in_delivery"],
            "in_delivery": ["delivery_completed"],
            "delivery_completed": ["waiting_pickup"]
        }
        return next in valid_transitions.get(current, [])
    
    def _on_state_changed(self, old_state: str, new_state: str):
        """배달원 특화 로직"""
        if new_state == "in_delivery":
            # 배달 시작 시간 기록
            DeliveryMetrics.record_delivery_start(self.entity_id)
            # 실시간 추적 활성화
            RealtimeTracking.activate(self.entity_id)
        elif new_state == "delivery_completed":
            # 배달 완료 시 정산 처리
            Settlement.process(self.entity_id)
            RealtimeTracking.deactivate(self.entity_id)
    
    def get_notification_message(self) -> str:
        messages = {
            "waiting_pickup": "픽업 준비 완료. 배달원을 기다리고 있습니다",
            "in_delivery": "배달이 시작되었습니다",
            "delivery_completed": "배달이 완료되었습니다"
        }
        return messages.get(self.state, "상태 변경됨")


# 편의점 클래스 (새로운 배송 주체 추가 - 확장 용이)
class ConvenienceStore(DeliveryEntity):
    def __init__(self, store_id: str):
        super().__init__(store_id)
        self.state = "receiving_order"
    
    def _is_valid_transition(self, current: str, next: str) -> bool:
        valid_transitions = {
            "receiving_order": ["packing", "out_of_stock"],
            "packing": ["ready_for_pickup"],
            "ready_for_pickup": ["pickup_completed"],
            "pickup_completed": [],
            "out_of_stock": []
        }
        return next in valid_transitions.get(current, [])
    
    def _on_state_changed(self, old_state: str, new_state: str):
        if new_state == "out_of_stock":
            RefundService.initiate_refund(self.entity_id)
        elif new_state == "ready_for_pickup":
            InventorySystem.reduce_stock(self.entity_id)
    
    def get_notification_message(self) -> str:
        messages = {
            "receiving_order": "주문을 접수했습니다",
            "packing": "상품을 준비 중입니다",
            "ready_for_pickup": "상품이 준비되었습니다",
            "pickup_completed": "픽업이 완료되었습니다",
            "out_of_stock": "죄송합니다. 상품이 품절되었습니다"
        }
        return messages.get(self.state, "상태 변경됨")
```

### 3단계: 다형성 활용 (단일 코드로 모든 주체 처리)

```python
# 핵심: 구체적 타입을 신경 쓰지 않고 추상 타입으로 처리
class OrderStateManager:
    def __init__(self):
        self.entities: dict[str, DeliveryEntity] = {}
    
    def register_entity(self, entity: DeliveryEntity):
        """모든 배송 주체를 동일하게 등록"""
        self.entities[entity.entity_id] = entity
    
    def process_state_change(self, entity_id: str, new_state: str):
        """구체적 타입을 모르고도 처리 - 다형성의 핵심"""
        entity = self.entities[entity_id]
        entity.change_state(new_state)  # 각 구현체의 메서드가 자동으로 호출됨
    
    def get_entity_status(self, entity_id: str) -> dict:
        """모든 주체

# 2026-05-07 학습 기록

## 📌 토픽

OOP 4대 원칙 (캡슐화, 상속, 다형성, 추상화)

## ❓ 질문 (3문제)

### 1. 📖 개념 설명

**캡슐화 관련 질문**

당신이 속한 팀에서 User 엔티티의 password 필드를 직접 노출하지 않고 setter를 막아두었습니다. 그런데 실무에서는 비밀번호 변경, 초기화, 암호화된 값 저장 등 여러 상황에서 password를 수정해야 합니다. 만약 이런 상황마다 updatePassword(), resetPassword(), setEncryptedPassword() 같은 메서드를 따로 만든다면, 메서드는 점점 늘어나고 관리가 복잡해집니다. 반면 그냥 password setter를 public으로 열어두면 어디서든 검증 없이 암호화되지 않은 평문이 저장될 수 있습니다. 이 사이에서 당신이라면 어떤 기준으로 캡슐화의 수준을 결정할 것인지, 그리고 메서드가 늘어나는 것이 정말 나쁜 설계인지 생각해보세요. 또한 JPA의 @Transient나 Hibernate의 @Formula 같은 도구들이 있는데, 이것들이 캡슐화 문제를 해결해주는 도구인지, 아니면 캡슐화를 우회하는 것인지도 함께 고민해보세요.

**상속 관련 질문**

당신의 팀에서 OrderService, PaymentService, ShippingService가 모두 "외부 API 호출 → 결과 로깅 → 트랜잭션 관리 → 에러 재시도"라는 공통 패턴을 반복하고 있습니다. 처음에는 BaseService라는 추상 클래스를 만들어서 이 패턴을 상속으로 해결했습니다. 그런데 시간이 지나면서 OrderService는 데이터베이스 조회도 필요하고, PaymentService는 암호화가 필요하고, ShippingService는 캐싱이 필요해졌습니다. 이제 BaseService에 계속 메서드를 추가할지, 아니면 각 Service마다 이 공통 로직을 다시 구현할지 고민됩니다. 더 나아가 Java는 단일 상속만 가능한데, 만약 다른 관점에서의 공통 기능(예: Auditable, Cacheable)도 필요하다면 어떻게 해야 할까요? 이 상황에서 상속의 장점과 한계를 어떻게 판단할 것인지, 그리고 실제로 당신이

### 2. 🔧 문제 상황

# OOP 4대 원칙 실무 장애 시나리오

당신이 속한 팀은 결제 시스템을 담당하고 있습니다. 카드, 계좌이체, 포인트 결제 등 다양한 결제 수단을 지원하는 Payment 모듈을 운영 중입니다.

***

## 시나리오: "결제 수단 추가 지옥에 빠진 팀"

지난 6개월간 결제 수단이 3개에서 9개로 늘었습니다(카드, 계좌, 포인트, 휴대폰, 간편결제 3종류). 매번 새로운 결제 수단을 추가할 때마다:

- 결제 서비스 메인 클래스가 600줄 → 1200줄로 팽창
- 결제 승인, 취소, 환불, 정산 등 메서드마다 if-else 체인이 길어짐
- 한 결제 수단의 버그 수정 시 다른 수단에 영향 없는지 회귀테스트 범위가 계속 확대
- 새로운 결제 수단 추가 시 기존 코드 수정 불가피 (OCP 위반)

현재 PaymentService.java의 구조:

```
PaymentService {
  public PaymentResult process(String paymentType, Payment payment) {
    if ("CARD".equals(paymentType)) {
      // 카드 결제 로직 50줄
    } else if ("TRANSFER".equals(paymentType)) {
      // 계좌이체 로직 60줄
    } else if ("POINT".equals(paymentType)) {
      // 포인트 로직 40줄
    } 
    // ... 계속 추가됨
  }
  
  public RefundResult refund(String paymentType, String transactionId) {
    if ("CARD".equals(paymentType)) {
      // 카드 환불
    } else if ("TRANSFER".equals(paymentType)) {
      // 계좌 환불
    }
    // ...
  }
  
  public SettlementResult settle(String paymentType, LocalDate date) {
    // 마찬가지로 길어짐
  }
}
```

***

## 실제 발생한 문제들

지난주 수요일 오전 9시, 운영 팀에서 긴급 알림:

"포인트 결제 취소가 안 됩니다. 고객이 취소 요청했는데 포인트가 환급 안 됨"

로그를 뒤져보니:
- 포인트 결제는 성공 (트랜잭션 1000건 일일 매출)
- 취소 요

### 3. 🎤 면접 질문

당신이 지금 개발 중인 전자상거래 시스템에서, 상품(Product), 전자책(EBook), 실물상품(PhysicalProduct), 구독상품(SubscriptionProduct) 같은 다양한 상품 타입이 있다고 가정해봅시다. 각 타입마다 가격 계산 로직, 재고 관리, 배송 처리 방식이 완전히 다릅니다.

현재 코드는 Product 엔티티 하나에 if-else로 이 모든 것을 처리하고 있는데, 새로운 상품 타입이 추가될 때마다 기존 코드를 수정해야 하고, 테스트도 복잡해졌습니다. 상속과 다형성을 활용해서 이 상황을 어떻게 개선할지 설명해주시고, 실제로 구현할 때 JPA와의 관계에서 어떤 선택을 했거나 하게 될지 얘기해주세요.


혹시 상속 구조를 설계하면서 SINGLE_TABLE 전략과 JOINED 전략 중에 고민한 적이 있나요? 이 상황에서 각각의 트레이드오프를 어떻게 봤는지 궁금합니다.

다형성으로 처리할 때 자식 클래스에서만 필요한 필드가 있으면 어떻게 처리하셨나요? 예를 들어 실물상품은 무게가, 전자책은 파일경로가 필요한데, null 체크로 처리했나요, 아니면 다른 방식을 썼나요?

지금 같은 구조에서 상품별 가격 계산 로직이 변경되거나 새로운 할인 정책이 생기면, Strategy 패턴이나 상속 기반의 메서드 오버라이딩 중 어떤 게 더 유지보수하기 좋았는지 경험상 얘기해주시겠어요?

만약 현재 운영 중인 서비스에서 기존 상품들의 저장 방식을 SINGLE_TABLE에서 JOINED로 변경해야 한다면, 마이그레이션을 어떻게 진행할지 고민해본 적이 있나요?

## 💬 나의 답변

상태

## 🤖 AI 피드백

(피드백 없음)

## 📚 모범 답안 & 개념 정리

### 1. 📖 개념 설명

# OOP 4대 원칙 심화 분석

## 1️⃣ 캡슐화 관련 질문

### 모범 답안

**핵심 관점: "캡슐화는 접근 제어가 아니라 책임 관리"**

이 딜레마는 **"정보 은닉의 본질이 무엇인가"**에 대한 질문입니다.

```java
// ❌ 나쁜 예: 접근 제어만 강조
public class User {
    private String password;
    // setter 완전 차단
}

// ❌ 또 다른 나쁜 예: 검증 없는 노출
public class User {
    public String password; // 평문 위험
}

// ✅ 올바른 접근: 책임을 명확히 하는 캡슐화
public class User {
    private EncryptedPassword password; // 값 객체로 감싸기
    
    // 단일 책임: password 변경만 담당
    public void changePassword(String rawPassword) {
        validatePasswordPolicy(rawPassword);
        this.password = EncryptedPassword.encrypt(rawPassword);
        this.passwordChangedAt = LocalDateTime.now();
    }
    
    // 관리자만 사용
    public void resetPassword(String tempPassword) {
        this.password = EncryptedPassword.encrypt(tempPassword);
        this.requiresPasswordChange = true;
    }
    
    // 내부 도메인 로직
    public boolean verifyPassword(String rawPassword) {
        return this.password.matches(rawPassword);
    }
    
    private void validatePasswordPolicy(String password) {
        if (password.length() < 8) throw new InvalidPasswordException();
        // 정책 검증
    }
}
```

**메서드가 여러 개인 것이 문제가 아닙니다.**
- `changePassword()` - 사용자 자신이 호출
- `resetPassword()` - 관리자/시스템이 호출
- `verifyPassword()` - 인증 로직에서 호출
- `forceChangePassword()` - 보안 정책에서 호출

→ 각 메서드는 **서로 다른 비즈니스 시나리오**를 표현합니다.

**@Transient와 @Formula에 대한 판단:**

```java
// @Transient: 캡슐화 우회 ⚠️
public class User {
    @Transient
    private String decryptedPassword; // DB에 저장 안 함
    
    // 문제: 이건 그냥 접근 제어 우회
}

// @Formula: 캡슐화 정책 표현 ✅
public class Order {
    @Formula("(SELECT COUNT(*) FROM order_items WHERE order_id = id)")
    private int itemCount;
    
    // 적절한 사용: 계산된 파생값을 읽기 전용으로 제공
}
```

### 핵심 개념 정리

- **정보 은닉 ≠ 접근 제어**
  - 접근 제어: private/public 키워드 수준
  - 정보 은닉: 내부 구현을 외부로부터 보호하는 설계 의도

- **값 객체(Value Object) 패턴**
  ```java
  public class EncryptedPassword {
      private final String encryptedValue;
      
      private EncryptedPassword(String encrypted) {
          this.encryptedValue = encrypted;
      }
      
      public static EncryptedPassword encrypt(String raw) {
          // 암호화 로직은 여기서만 관리
          return new EncryptedPassword(hashPassword(raw));
      }
      
      public boolean matches(String raw) {
          return this.encryptedValue.equals(hashPassword(raw));
      }
  }
  ```

- **메서드 폭발(Method Explosion) vs 책임 분리**
  - 메서드가 많다 = 비즈니스 케이스가 다양하다 (OK)
  - 메서드가 모호하다 = 단일 책임 위반 (NOT OK)

- **접근 제어 전략**
  | 수준 | 사용 시점 | 예시 |
  |------|---------|------|
  | private | 내부 구현 | 검증 로직, 헬퍼 메서드 |
  | package-private | 도메인 같은 패키지 | 엔티티 간 통신 |
  | protected | 상속 계층 | 공통 템플릿 메서드 |
  | public | 공개 계약 | 도메인 행동 |

### 실무 포인트

```java
// 🔴 초기 설계 (실패 사례)
public class User {
    private String password;
    // setter가 없어서 비밀번호를 바꿀 수 없음!
    // 해결: SQL UPDATE로 직접 수정? → 도메인 로직 우회
}

// 🟢 개선된 설계
public class User implements AggregateRoot {
    private EncryptedPassword password;
    private PasswordChangeHistory history;
    
    // 도메인 이벤트 발행
    public void changePassword(String oldPassword, String newPassword) {
        if (!password.matches(oldPassword)) {
            throw new InvalidPasswordException();
        }
        validatePasswordPolicy(newPassword);
        
        EncryptedPassword newEncrypted = EncryptedPassword.encrypt(newPassword);
        this.password = newEncrypted;
        this.history.add(newEncrypted, LocalDateTime.now());
        
        // DDD 패턴: 도메인 이벤트 발행
        addDomainEvent(new PasswordChangedEvent(this.id, LocalDateTime.now()));
    }
}

// 스프링 예제
@Service
public class UserService {
    @Transactional
    public void changePassword(Long userId, String oldPassword, String newPassword) {
        User user = userRepository.findById(userId)
            .orElseThrow(() -> new UserNotFoundException());
        
        // User 엔티티가 자신의 책임을 다함
        user.changePassword(oldPassword, newPassword);
        
        // 저장만 함 (도메인 로직은 User에서)
        userRepository.save(user);
    }
}
```

**팀 컨벤션 예시:**
```
1. password 같은 민감정보는 항상 값 객체로 감싼다
2. 암호화/해시는 값 객체 내부에서만 처리한다
3. 비즈니스 시나리오마다 명확한 메서드명을 사용한다
4. @Transient는 성능 최적화, @Formula는 읽기 전용 파생값
```

---

## 2️⃣ 상속 관련 질문

### 모범 답안

**핵심 관점: "상속은 'is-a' 관계이고, 공통 로직은 'has-a'로"**

이것은 **상속의 한계를 인식하고 조합(Composition)으로 해결**하는 문제입니다.

```java
// ❌ 안티패턴: 기능 기반 상속
public abstract class BaseService {
    protected void callExternalApi() { }
    protected void logResult() { }
    protected void manageTransaction() { }
    protected void retryOnError() { }
    protected void queryDatabase() { }    // OrderService만 필요
    protected void encryptData() { }      // PaymentService만 필요
    protected void cacheResult() { }      // ShippingService만 필요
}

// OrderService는 database가 필요 없는데도 상속받음
// 이를 "Fragile Base Class Problem"이라 부름

// ✅ 올바른 접근: 기능을 조합 가능한 객체로 분해
public interface ServiceOperation {
    <T> T execute(ServiceRequest request);
}

// 단일 책임, 조합 가능
public class ExternalApiCaller implements ServiceOperation {
    private final RestTemplate restTemplate;
    
    @Override
    public Object execute(ServiceRequest request) {
        return restTemplate.postForObject(request.getUrl(), request.getBody(), Object.class);
    }
}

public class ResultLogger implements ServiceOperation {
    private final Logger logger;
    
    @Override
    public Object execute(ServiceRequest request) {
        logger.info("Request: {}", request);
        return request;
    }
}

public class RetryHandler implements ServiceOperation {
    private final int maxRetries;
    private final long backoffMs;
    
    @Override
    public Object execute(ServiceRequest request) {
        for (int i = 0; i < maxRetries; i++) {
            try {
                return request; // 실제로는 다음 operation 호출
            } catch (Exception e) {
                if (i == maxRetries - 1) throw e;

---

### 2. 🔧 문제 상황

# OOP 4대 원칙 실무 장애 시나리오 - 모범 답안

## 모범 답안

### 문제 분석: OOP 원칙 위반 진단

현재 코드는 **다형성 부재**, **캡슐화 미흡**, **단일 책임 원칙 위반**으로 인한 전형적인 레거시 패턴입니다.

### 해결책: OOP 4대 원칙 적용한 리팩토링

```java
// 1️⃣ 추상화: 결제 수단의 공통 인터페이스 정의
public interface PaymentMethod {
    PaymentResult process(PaymentRequest request);
    RefundResult refund(String transactionId, BigDecimal amount);
    SettlementResult settle(LocalDate date);
    boolean validate(PaymentRequest request);
}

// 2️⃣ 캡슐화: 각 결제 수단별 비즈니스 로직을 자체 클래스에 캡슐화
public class CardPayment implements PaymentMethod {
    private CardGateway gateway;
    private CardValidator validator;
    
    @Override
    public PaymentResult process(PaymentRequest request) {
        // 카드 결제 로직 캡슐화
        if (!validator.validate(request)) {
            return PaymentResult.fail("Invalid card");
        }
        
        CardTransaction transaction = gateway.authorize(
            request.getCardNumber(),
            request.getAmount()
        );
        
        return PaymentResult.success(transaction.getId());
    }
    
    @Override
    public RefundResult refund(String transactionId, BigDecimal amount) {
        // 카드 환불은 카드 전문가만 알고 있음
        return gateway.refund(transactionId, amount);
    }
    
    @Override
    public SettlementResult settle(LocalDate date) {
        return gateway.dailySettle(date);
    }
}

public class PointPayment implements PaymentMethod {
    private PointService pointService;
    private PointValidator validator;
    
    @Override
    public PaymentResult process(PaymentRequest request) {
        // 포인트 결제 로직
        if (!validator.hasEnoughPoint(request.getMemberId(), request.getAmount())) {
            return PaymentResult.fail("Insufficient points");
        }
        
        String transactionId = pointService.deductPoint(
            request.getMemberId(),
            request.getAmount()
        );
        
        return PaymentResult.success(transactionId);
    }
    
    @Override
    public RefundResult refund(String transactionId, BigDecimal amount) {
        // 포인트 환불: 차감했던 포인트 복구
        pointService.restorePoint(transactionId, amount);
        return RefundResult.success();
    }
    
    @Override
    public SettlementResult settle(LocalDate date) {
        return pointService.settlementReport(date);
    }
}

// 3️⃣ 상속 활용 (선택적): 공통 기능이 있다면 추상 클래스 활용
public abstract class PaymentMethodAdapter implements PaymentMethod {
    protected Logger logger = LoggerFactory.getLogger(getClass());
    protected MetricsCollector metrics;
    
    // 공통 로깅, 모니터링 기능
    protected void logTransaction(String transactionId, BigDecimal amount) {
        logger.info("[{}] Transaction: {}, Amount: {}", 
            getClass().getSimpleName(), transactionId, amount);
        metrics.recordTransaction(getClass().getSimpleName(), amount);
    }
    
    // 공통 재시도 로직
    protected <T> T executeWithRetry(Supplier<T> operation, int retries) {
        // 공통 재시도 로직
        return null;
    }
}

// 4️⃣ 다형성: PaymentService는 추상화에만 의존
public class PaymentService {
    private Map<PaymentType, PaymentMethod> paymentMethods;
    
    public PaymentService() {
        this.paymentMethods = new HashMap<>();
        // 결제 수단 등록 (팩토리 패턴 활용)
        paymentMethods.put(PaymentType.CARD, new CardPayment(cardGateway));
        paymentMethods.put(PaymentType.TRANSFER, new TransferPayment(transferGateway));
        paymentMethods.put(PaymentType.POINT, new PointPayment(pointService));
        paymentMethods.put(PaymentType.MOBILE, new MobilePayment(mobileGateway));
    }
    
    // 다형성: 구체적 결제 수단에 신경 쓰지 않음
    public PaymentResult process(PaymentRequest request) {
        PaymentMethod method = paymentMethods.get(request.getType());
        
        if (method == null) {
            throw new UnsupportedPaymentMethodException(request.getType());
        }
        
        return method.process(request);
    }
    
    public RefundResult refund(PaymentRequest request) {
        PaymentMethod method = paymentMethods.get(request.getType());
        return method.refund(request.getTransactionId(), request.getAmount());
    }
    
    public SettlementResult settle(PaymentType type, LocalDate date) {
        PaymentMethod method = paymentMethods.get(type);
        return method.settle(date);
    }
}

// 5️⃣ 새로운 결제 수단 추가 (기존 코드 수정 없음 - OCP 준수)
public class CryptoPayment implements PaymentMethod {
    private BlockchainGateway gateway;
    
    @Override
    public PaymentResult process(PaymentRequest request) {
        // 암호화폐 결제 로직
        return null;
    }
    
    @Override
    public RefundResult refund(String transactionId, BigDecimal amount) {
        return null;
    }
    
    @Override
    public SettlementResult settle(LocalDate date) {
        return null;
    }
}

// 등록 시
paymentMethods.put(PaymentType.CRYPTO, new CryptoPayment(blockchainGateway));
```

### 포인트 결제 환불 버그 분석 및 수정

```java
public class PointPayment implements PaymentMethod {
    private PointService pointService;
    
    @Override
    public RefundResult refund(String transactionId, BigDecimal amount) {
        try {
            // ❌ 버그: 환불 전 원래 거래 검증 없음
            pointService.restorePoint(transactionId, amount);
            
            // ❌ 버그: 환불 실패 시 재시도 로직 없음
            // ❌ 버그: 환불 상태가 DB에 기록되지 않음
            
            return RefundResult.success();
        } catch (Exception e) {
            logger.error("Refund failed for transaction: {}", transactionId);
            throw new RefundProcessingException(e);
        }
    }
}

// ✅ 수정된 버전
public class PointPayment implements PaymentMethod {
    
    @Override
    public RefundResult refund(String transactionId, BigDecimal amount) {
        // 1. 원래 거래 조회 및 상태 검증
        PointTransaction originalTransaction = pointService
            .getTransaction(transactionId)
            .orElseThrow(() -> new TransactionNotFoundException(transactionId));
        
        if (!originalTransaction.canRefund()) {
            return RefundResult.fail("Cannot refund already refunded transaction");
        }
        
        // 2. 환불 트랜잭션 생성 (원자성 보장)
        try {
            String refundId = pointService.createRefundTransaction(
                transactionId,
                amount,
                RefundStatus.PENDING
            );
            
            // 3. 포인트 복구 실행
            pointService.restorePoint(originalTransaction.getMemberId(), amount);
            
            // 4. 환불 상태 완료로 업데이트
            pointService.updateRefundStatus(refundId, RefundStatus.COMPLETED);
            
            logger.info("Point refund completed: {} -> {}", transactionId, refundId);
            return RefundResult.success(refundId);
            
        } catch (Exception e) {
            logger.error("Point refund failed: {}", transactionId, e);
            return RefundResult.fail("Refund processing error: " + e.getMessage());
        }
    }
}
```

---

## 핵심 개념 정리

### 1️⃣ 추상화 (Abstraction)
- **정의**: 복잡한 구현을 숨기고 핵심 기능만 노출
- **적용**: `PaymentMethod` 인터페이스로 모든 결제 수단이 따라야 할 계약 정의
- **효과**: 결제 수단의 차이를 무시하고 균일하게 처리 가능

```
┌─────────────────────────────────┐
│     PaymentMethod (추상화)       │
│  - process()                     │
│  - ref

---

### 3. 🎤 면접 질문

# OOP 4대 원칙을 활용한 전자상거래 시스템 리팩토링

## 모범 답안

### 1️⃣ 상속과 다형성을 활용한 구조 개선

**현재 문제점:**
```java
// ❌ 안티패턴: if-else 지옥
public class Product {
    private String type; // "EBOOK", "PHYSICAL", "SUBSCRIPTION"
    
    public BigDecimal calculatePrice() {
        if ("EBOOK".equals(type)) {
            // 전자책 가격 계산
        } else if ("PHYSICAL".equals(type)) {
            // 실물 가격 계산
        } else if ("SUBSCRIPTION".equals(type)) {
            // 구독 가격 계산
        }
        return price;
    }
    
    public void processShipping() {
        if ("EBOOK".equals(type)) {
            // 배송 불필요
        } else if ("PHYSICAL".equals(type)) {
            // 배송 처리
        }
    }
}
```

**개선된 구조 (다형성 활용):**

```java
// 추상 기본 클래스: 추상화 원칙 적용
@Entity
@Inheritance(strategy = InheritanceType.JOINED)
@DiscriminatorColumn(name = "product_type")
public abstract class Product {
    @Id
    private Long id;
    
    private String name;
    private BigDecimal basePrice;
    private LocalDateTime createdAt;
    
    // 추상 메서드: 각 타입이 반드시 구현해야 함
    public abstract BigDecimal calculatePrice(OrderContext context);
    public abstract ShippingInfo getShippingInfo();
    public abstract InventoryStrategy getInventoryStrategy();
    
    // 공통 로직은 상위 클래스에서 구현
    public final Order createOrder(OrderContext context) {
        BigDecimal finalPrice = calculatePrice(context);
        ShippingInfo shipping = getShippingInfo();
        return new Order(this, finalPrice, shipping);
    }
}

// 전자책: 각 타입별 구체적 구현
@Entity
@DiscriminatorValue("EBOOK")
public class EBook extends Product {
    @Column(name = "file_path")
    private String filePath;
    
    @Column(name = "file_size_mb")
    private Integer fileSizeMb;
    
    @Override
    public BigDecimal calculatePrice(OrderContext context) {
        // 전자책은 항상 동일한 가격
        return getBasePrice();
    }
    
    @Override
    public ShippingInfo getShippingInfo() {
        // 전자책은 즉시 다운로드
        return ShippingInfo.builder()
            .method(ShippingMethod.INSTANT_DOWNLOAD)
            .estimatedDays(0)
            .cost(BigDecimal.ZERO)
            .build();
    }
    
    @Override
    public InventoryStrategy getInventoryStrategy() {
        return new UnlimitedInventoryStrategy();
    }
}

// 실물상품
@Entity
@DiscriminatorValue("PHYSICAL")
public class PhysicalProduct extends Product {
    @Column(name = "weight_kg")
    private BigDecimal weightKg;
    
    @Column(name = "stock_quantity")
    private Integer stockQuantity;
    
    @Embedded
    private Dimensions dimensions;
    
    @Override
    public BigDecimal calculatePrice(OrderContext context) {
        BigDecimal basePrice = getBasePrice();
        BigDecimal shippingCost = calculateShippingCost(context.getAddress());
        
        // 지역별 추가 비용 계산
        return basePrice.add(shippingCost);
    }
    
    @Override
    public ShippingInfo getShippingInfo() {
        return ShippingInfo.builder()
            .method(ShippingMethod.PARCEL)
            .estimatedDays(3)
            .needsPackaging(true)
            .build();
    }
    
    @Override
    public InventoryStrategy getInventoryStrategy() {
        return new LimitedInventoryStrategy(stockQuantity);
    }
    
    private BigDecimal calculateShippingCost(Address address) {
        // 무게와 거리를 기반으로 배송비 계산
        return weightKg.multiply(new BigDecimal("2000"))
                       .add(calculateDistanceFee(address));
    }
}

// 구독상품
@Entity
@DiscriminatorValue("SUBSCRIPTION")
public class SubscriptionProduct extends Product {
    @Column(name = "billing_period_days")
    private Integer billingPeriodDays;
    
    @Column(name = "auto_renewal")
    private Boolean autoRenewal;
    
    @OneToMany(mappedBy = "subscriptionProduct")
    private List<SubscriptionTier> tiers;
    
    @Override
    public BigDecimal calculatePrice(OrderContext context) {
        // 구독 단계별 가격 계산
        SubscriptionTier selectedTier = context.getSelectedTier();
        return selectedTier.getMonthlyPrice();
    }
    
    @Override
    public ShippingInfo getShippingInfo() {
        return ShippingInfo.builder()
            .method(ShippingMethod.DIGITAL_ACCESS)
            .estimatedDays(0)
            .cost(BigDecimal.ZERO)
            .build();
    }
    
    @Override
    public InventoryStrategy getInventoryStrategy() {
        return new SubscriptionInventoryStrategy();
    }
}
```

### 2️⃣ JPA 상속 전략: SINGLE_TABLE vs JOINED

**JOINED 전략 선택 이유 (제 선택):**

```java
// ✅ JOINED 전략 (권장)
@Entity
@Inheritance(strategy = InheritanceType.JOINED)
@DiscriminatorColumn(name = "product_type")
public abstract class Product { ... }

// 생성되는 테이블 구조:
// - product (id, name, base_price, product_type) [부모]
// - ebook (id, file_path, file_size_mb) [자식]
// - physical_product (id, weight_kg, stock_quantity) [자식]
// - subscription_product (id, billing_period_days, auto_renewal) [자식]
```

**트레이드오프 분석:**

| 항목 | JOINED | SINGLE_TABLE | 선택 근거 |
|------|--------|--------------|----------|
| **쿼리 성능** | JOIN 필요 (느림) | 단일 테이블 (빠름) | 정규화 중요 |
| **데이터 정규화** | 우수 | 나쁨 (NULL 많음) | 무결성 중요 |
| **스토리지** | 효율적 | 비효율적 | 저장공간 절약 |
| **새 타입 추가** | 쉬움 | 쉬움 | 유지보수 편함 |
| **타입별 컬럼** | 테이블 분리 | 모두 한 테이블 | 논리적 분리 |
| **복잡한 조회** | 복잡도 증가 | 간단 | 현실적 고려 |

```java
// ❌ SINGLE_TABLE (비권장 이유)
@Entity
@Inheritance(strategy = InheritanceType.SINGLE_TABLE)
public abstract class Product {
    // product 테이블에 모든 컬럼이 혼재
    // - id, name, base_price, product_type (공통)
    // - file_path (EBOOK만 사용)
    // - weight_kg, stock_quantity (PHYSICAL만 사용)
    // - billing_period_days (SUBSCRIPTION만 사용)
    // → 많은 NULL 값, 데이터 무결성 문제
}

// 실무에서는 JOINED를 선택하되, N+1 쿼리 문제 대비
```

### 3️⃣ 자식 클래스 전용 필드 처리

**방법 1: Repository 레벨의 명시적 처리 (권장)**

```java
public interface ProductRepository extends JpaRepository<Product, Long> {
    // 타입별 조회 쿼리
    @Query("SELECT p FROM EBook p WHERE p.id = :id")
    Optional<EBook> findEBookById(@Param("id") Long id);
    
    @Query("SELECT p FROM PhysicalProduct p WHERE p.id = :id")
    Optional<PhysicalProduct> findPhysicalProductById(@Param("id") Long id);
}

// Service 계층에서 명시적 캐스팅
@Service
public class ProductService {
    public String getProductFileInfo(Long productId) {
        EBook ebook = productRepository.findEBookById(productId)
            .orElseThrow(() -> new ProductNotFoundException("EBook not found"));
        
        return ebook.getFilePath(); // null 체크 불필요
    }
    
    public Integer getProductStock(Long productId) {
        PhysicalProduct physical = productRepository.findPhysicalProductById(productId)
            .orElseThrow(() -> new ProductNotFoundException("

# 2026-05-07 학습 기록

## 📌 토픽

OOP 4대 원칙 (캡슐화, 상속, 다형성, 추상화)

## ❓ 질문 (3문제)

### 1. 📖 개념 설명

**캡슐화 관련 질문**

당신이 속한 팀에서 User 엔티티의 password 필드를 직접 노출하지 않고 setter를 막아두었습니다. 그런데 실무에서는 비밀번호 변경, 초기화, 암호화된 값 저장 등 여러 상황에서 password를 수정해야 합니다. 만약 이런 상황마다 updatePassword(), resetPassword(), setEncryptedPassword() 같은 메서드를 따로 만든다면, 메서드는 점점 늘어나고 관리가 복잡해집니다. 반면 그냥 password setter를 public으로 열어두면 어디서든 검증 없이 암호화되지 않은 평문이 저장될 수 있습니다. 이 사이에서 당신이라면 어떤 기준으로 캡슐화의 수준을 결정할 것인지, 그리고 메서드가 늘어나는 것이 정말 나쁜 설계인지 생각해보세요. 또한 JPA의 @Transient나 Hibernate의 @Formula 같은 도구들이 있는데, 이것들이 캡슐화 문제를 해결해주는 도구인지, 아니면 캡슐화를 우회하는 것인지도 함께 고민해보세요.

**상속 관련 질문**

당신의 팀에서 OrderService, PaymentService, ShippingService가 모두 "외부 API 호출 → 결과 로깅 → 트랜잭션 관리 → 에러 재시도"라는 공통 패턴을 반복하고 있습니다. 처음에는 BaseService라는 추상 클래스를 만들어서 이 패턴을 상속으로 해결했습니다. 그런데 시간이 지나면서 OrderService는 데이터베이스 조회도 필요하고, PaymentService는 암호화가 필요하고, ShippingService는 캐싱이 필요해졌습니다. 이제 BaseService에 계속 메서드를 추가할지, 아니면 각 Service마다 이 공통 로직을 다시 구현할지 고민됩니다. 더 나아가 Java는 단일 상속만 가능한데, 만약 다른 관점에서의 공통 기능(예: Auditable, Cacheable)도 필요하다면 어떻게 해야 할까요? 이 상황에서 상속의 장점과 한계를 어떻게 판단할 것인지, 그리고 실제로 당신이

### 2. 🔧 문제 상황

# OOP 4대 원칙 실무 장애 시나리오

당신이 속한 팀은 결제 시스템을 담당하고 있습니다. 카드, 계좌이체, 포인트 결제 등 다양한 결제 수단을 지원하는 Payment 모듈을 운영 중입니다.

***

## 시나리오: "결제 수단 추가 지옥에 빠진 팀"

지난 6개월간 결제 수단이 3개에서 9개로 늘었습니다(카드, 계좌, 포인트, 휴대폰, 간편결제 3종류). 매번 새로운 결제 수단을 추가할 때마다:

- 결제 서비스 메인 클래스가 600줄 → 1200줄로 팽창
- 결제 승인, 취소, 환불, 정산 등 메서드마다 if-else 체인이 길어짐
- 한 결제 수단의 버그 수정 시 다른 수단에 영향 없는지 회귀테스트 범위가 계속 확대
- 새로운 결제 수단 추가 시 기존 코드 수정 불가피 (OCP 위반)

현재 PaymentService.java의 구조:

```
PaymentService {
  public PaymentResult process(String paymentType, Payment payment) {
    if ("CARD".equals(paymentType)) {
      // 카드 결제 로직 50줄
    } else if ("TRANSFER".equals(paymentType)) {
      // 계좌이체 로직 60줄
    } else if ("POINT".equals(paymentType)) {
      // 포인트 로직 40줄
    } 
    // ... 계속 추가됨
  }
  
  public RefundResult refund(String paymentType, String transactionId) {
    if ("CARD".equals(paymentType)) {
      // 카드 환불
    } else if ("TRANSFER".equals(paymentType)) {
      // 계좌 환불
    }
    // ...
  }
  
  public SettlementResult settle(String paymentType, LocalDate date) {
    // 마찬가지로 길어짐
  }
}
```

***

## 실제 발생한 문제들

지난주 수요일 오전 9시, 운영 팀에서 긴급 알림:

"포인트 결제 취소가 안 됩니다. 고객이 취소 요청했는데 포인트가 환급 안 됨"

로그를 뒤져보니:
- 포인트 결제는 성공 (트랜잭션 1000건 일일 매출)
- 취소 요

### 3. 🎤 면접 질문

당신이 지금 개발 중인 전자상거래 시스템에서, 상품(Product), 전자책(EBook), 실물상품(PhysicalProduct), 구독상품(SubscriptionProduct) 같은 다양한 상품 타입이 있다고 가정해봅시다. 각 타입마다 가격 계산 로직, 재고 관리, 배송 처리 방식이 완전히 다릅니다.

현재 코드는 Product 엔티티 하나에 if-else로 이 모든 것을 처리하고 있는데, 새로운 상품 타입이 추가될 때마다 기존 코드를 수정해야 하고, 테스트도 복잡해졌습니다. 상속과 다형성을 활용해서 이 상황을 어떻게 개선할지 설명해주시고, 실제로 구현할 때 JPA와의 관계에서 어떤 선택을 했거나 하게 될지 얘기해주세요.


혹시 상속 구조를 설계하면서 SINGLE_TABLE 전략과 JOINED 전략 중에 고민한 적이 있나요? 이 상황에서 각각의 트레이드오프를 어떻게 봤는지 궁금합니다.

다형성으로 처리할 때 자식 클래스에서만 필요한 필드가 있으면 어떻게 처리하셨나요? 예를 들어 실물상품은 무게가, 전자책은 파일경로가 필요한데, null 체크로 처리했나요, 아니면 다른 방식을 썼나요?

지금 같은 구조에서 상품별 가격 계산 로직이 변경되거나 새로운 할인 정책이 생기면, Strategy 패턴이나 상속 기반의 메서드 오버라이딩 중 어떤 게 더 유지보수하기 좋았는지 경험상 얘기해주시겠어요?

만약 현재 운영 중인 서비스에서 기존 상품들의 저장 방식을 SINGLE_TABLE에서 JOINED로 변경해야 한다면, 마이그레이션을 어떻게 진행할지 고민해본 적이 있나요?

## 💬 나의 답변

상태

## 🤖 AI 피드백

(피드백 없음)

## 📚 모범 답안 & 개념 정리

### 1. 📖 개념 설명

# OOP 4대 원칙 심화 Q&A

## 📌 캡슐화 관련 질문

### 모범 답안

**캡슐화의 수준 결정 기준:**

캡슐화는 "정보 은닉"이 목표이지, "무조건 접근을 막는 것"이 아닙니다. 이 문제의 핵심은 **책임과 의도의 명확성**에 있습니다.

```java
// ❌ 나쁜 예: 과도한 메서드 증식
public void updatePassword(String newPassword) { ... }
public void resetPassword() { ... }
public void setEncryptedPassword(String encrypted) { ... }
public void setTemporaryPassword() { ... }
// → 메서드 증식, 책임이 분산됨

// ✅ 좋은 예: 책임 있는 setter + 내부 검증
public class User {
    private String password; // private 유지
    
    // 단일 진입점, 모든 변경이 여기를 통과
    public void changePassword(String rawPassword) {
        if (!isValidPassword(rawPassword)) {
            throw new InvalidPasswordException("규칙 미충족");
        }
        this.password = hashPassword(rawPassword); // 암호화
        this.lastPasswordChangedAt = LocalDateTime.now(); // 감사추적
    }
    
    // 관리자 초기화 - 의도가 명확한 별도 메서드
    public void resetPasswordByAdmin(String tempPassword) {
        this.password = hashPassword(tempPassword);
        this.requiresPasswordChange = true; // 다음 로그인 시 강제 변경
    }
    
    // 내부용: 이미 해시된 값을 설정 (테스트, 마이그레이션용)
    void setPasswordHash(String hash) {
        this.password = hash;
    }
}
```

**메서드 증식이 정말 나쁜 설계인가?**

아닙니다. **목적이 명확하다면** 메서드 증식은 건강한 신호입니다:
- `changePassword()` - 사용자가 스스로 변경
- `resetPasswordByAdmin()` - 관리자가 초기화
- `setPasswordHash()` - 내부 유틸리티 (package-private)

각 메서드는 **다른 검증 규칙과 부수효과(side effects)**를 가집니다.

**@Transient와 @Formula의 역할:**

```java
public class User {
    @Column
    private String passwordHash;
    
    // ✅ @Transient: DB에 저장하지 않음
    // 캡슐화 ○, 성능 ○
    @Transient
    private String rawPassword; // 메모리상에만 존재
    
    // ❌ @Formula: 쿼리 시마다 자동 계산
    // 캡슐화는 충족하지만, 성능 문제 발생 가능
    @Formula("CASE WHEN last_login > NOW() - INTERVAL 7 DAY THEN 1 ELSE 0 END")
    private boolean activeThisWeek;
}
```

**결론:** 이들은 **캡슐화를 우회하는 것이 아니라 지원하는 도구**입니다. 진정한 캡슐화는 "어떤 데이터를 어떻게 변경할 것인가"의 의도를 명확히 하는 것입니다.

---

### 핵심 개념 정리

#### 캡슐화(Encapsulation)
- **정보 은닉 ≠ 접근 차단**: 불필요한 세부사항만 숨기기
- **변경의 의도를 명확히**: 메서드명으로 목적 표현 (changePassword vs resetPassword)
- **불변식(Invariant) 보호**: 객체가 항상 유효한 상태 유지
  ```java
  private LocalDateTime lastPasswordChangedAt;
  // password 변경 시 반드시 함께 업데이트 → 불변식 보호
  ```

#### 검증 계층화
| 레벨 | 책임 | 예시 |
|------|------|------|
| **Public** | 비즈니스 검증 | `changePassword()` - 정책 적용 |
| **Protected/Package** | 내부 접근 | `setPasswordHash()` - 테스트 |
| **Private** | 내부 유틸리티 | `hashPassword()` - 구현 |

#### 접근 제어자 전략
```java
// 계층적 접근 제어
public changePassword()        // 외부 사용자
protected resetPasswordByAdmin() // 같은 패키지 서비스
package-private setPasswordHash() // 테스트, 마이그레이션
private hashPassword()         // 구현 세부사항
```

---

### 실무 포인트

#### ✅ DO: 캡슐화의 올바른 사용
```java
@Entity
public class User {
    private String password;
    
    // 단일 책임, 명확한 의도
    public void changePassword(String currentPassword, String newPassword) {
        validateCurrentPassword(currentPassword);
        validateNewPasswordPolicy(newPassword);
        validateNotReusedInLast3Months(newPassword);
        
        this.password = PasswordEncoder.encode(newPassword);
        this.lastPasswordChangedAt = LocalDateTime.now();
        this.passwordChangeCount++;
    }
}

// 사용
user.changePassword("oldPwd", "newSecurePwd123!");
```

#### ❌ DON'T: 캡슐화 위반
```java
// 위험: setter가 검증 없이 평문 저장
user.setPassword("plaintext");

// 위험: 너무 많은 변형 메서드
user.updatePassword();
user.modifyPassword();
user.setPassword();
user.resetPassword();
```

#### 🎯 트레이드오프 판단 기준
```
결정: changePassword() 하나 vs 여러 메서드

메서드 많음 (+)          메서드 하나 (+)
├─ 의도 명확             ├─ 심플함
├─ 각자 검증 로직        └─ 모든 변경점 추적 용이
└─ 의도치 않은 호출 방지

→ 결론: 의도가 다르면 메서드를 나누되,
  모든 변경이 필수 검증을 통과하도록 보장
```

---

---

## 📌 상속 관련 질문

### 모범 답안

**현재 상황 분석:**

BaseService 상속은 처음에는 DRY(Don't Repeat Yourself) 원칙에 좋아 보이지만, **시간이 지나며 3가지 문제 발생**:

1. **기능 증식으로 인한 God Object**: 모든 서비스에 필요 없는 메서드가 포함됨
2. **단일 상속의 한계**: Auditable, Cacheable 같은 다른 관점의 기능을 추가할 수 없음
3. **IS-A vs HAS-A 혼동**: 상속이 진짜 관계인지 확인 필요

**올바른 해결책:**

```java
// ❌ 상속으로 모든 공통 로직을 강제
public abstract class BaseService {
    protected void executeWithRetry(Callable<?> action) { ... }
    protected void logResult(String result) { ... }
    protected void manageTransaction() { ... }
    protected void encrypt() { ... }      // PaymentService만 필요
    protected void cache() { ... }        // ShippingService만 필요
    protected void queryDatabase() { ... } // OrderService만 필요
}

// ✅ 조합(Composition)으로 필요한 기능만 선택
public interface ApiCallHandler {
    <T> T executeWithRetry(Callable<T> action);
}

public interface ResultLogger {
    void logResult(String context, Object result);
}

public interface TransactionManager {
    void executeInTransaction(Runnable action);
}

// 각 서비스는 필요한 기능만 주입
@Service
public class OrderService {
    private final ApiCallHandler apiHandler;
    private final ResultLogger logger;
    private final TransactionManager txManager;
    private final DatabaseRepository dbRepo;
    
    public void processOrder(Long orderId) {
        Order order = apiHandler.executeWithRetry(
            () -> dbRepo.findById(orderId)
        );
        
        txManager.executeInTransaction(() -> {
            // 주문 처리 로직
            logger.logResult("ORDER_PROCESS", order);
        });
    }
}

@Service
public class PaymentService {
    private final ApiCallHandler apiHandler;
    private

---

### 2. 🔧 문제 상황

# OOP 4대 원칙 실무 장애 시나리오 - 모범 답안

## 모범 답안

### 1️⃣ 문제 분석: OOP 원칙 위반 진단

현재 코드는 **4가지 OOP 원칙 모두를 위반**하고 있습니다:

| 원칙 | 위반 형태 | 영향 |
|------|---------|------|
| **추상화** | 결제 수단의 공통 인터페이스 부재 | 각 수단별 로직이 산재됨 |
| **캡슐화** | 결제 로직이 PaymentService에 노출 | 책임 분산, 단일책임원칙 위반 |
| **상속/다형성** | if-else 체인으로 타입 판별 | 새 수단 추가 시 기존 코드 수정 필수 (OCP 위반) |
| **OCP 위반** | 확장에 닫혀있고 수정에 열려있음 | 회귀테스트 범위 확대, 버그 리스크 증가 |

### 2️⃣ 해결책: Strategy + Template Method 패턴 적용

```java
// 1단계: 추상화 - 결제 수단의 공통 계약 정의
public abstract class PaymentStrategy {
    protected String paymentType;
    
    public abstract PaymentResult process(Payment payment);
    public abstract RefundResult refund(String transactionId, BigDecimal amount);
    public abstract SettlementResult settle(LocalDate date);
    public abstract void validate(Payment payment) throws InvalidPaymentException;
    
    // Template Method Pattern: 공통 흐름 정의
    public final PaymentResult executePayment(Payment payment) {
        validate(payment);
        return process(payment);
    }
}

// 2단계: 구체적 구현 - 각 결제 수단 전담 클래스
public class CardPaymentStrategy extends PaymentStrategy {
    @Autowired
    private CardGateway cardGateway;
    
    @Override
    public PaymentResult process(Payment payment) {
        CardPaymentRequest request = CardPaymentRequest.from(payment);
        CardPaymentResponse response = cardGateway.authorize(request);
        
        return PaymentResult.builder()
            .transactionId(response.getTransactionId())
            .status(PaymentStatus.SUCCESS)
            .build();
    }
    
    @Override
    public RefundResult refund(String transactionId, BigDecimal amount) {
        return cardGateway.refund(transactionId, amount);
    }
    
    @Override
    public SettlementResult settle(LocalDate date) {
        return cardGateway.settle(date);
    }
    
    @Override
    public void validate(Payment payment) throws InvalidPaymentException {
        if (payment.getCard() == null) {
            throw new InvalidPaymentException("Card information is required");
        }
        if (!payment.getCard().isValid()) {
            throw new InvalidPaymentException("Invalid card");
        }
    }
}

public class PointPaymentStrategy extends PaymentStrategy {
    @Autowired
    private PointGateway pointGateway;
    @Autowired
    private PointLedgerRepository ledgerRepo;
    
    @Override
    public PaymentResult process(Payment payment) {
        // 포인트 차감 트랜잭션
        PointDeductionResult result = pointGateway.deduct(
            payment.getMemberId(), 
            payment.getAmount()
        );
        
        // 장부 기록 (감시 추적용)
        ledgerRepo.save(PointLedger.ofDeduction(payment, result));
        
        return PaymentResult.builder()
            .transactionId(result.getTransactionId())
            .status(PaymentStatus.SUCCESS)
            .build();
    }
    
    @Override
    public RefundResult refund(String transactionId, BigDecimal amount) {
        // 포인트 환급 (여기서 발생했던 버그!)
        PointRefundResult result = pointGateway.refund(transactionId, amount);
        ledgerRepo.save(PointLedger.ofRefund(transactionId, result));
        return result;
    }
    
    @Override
    public void validate(Payment payment) throws InvalidPaymentException {
        BigDecimal balance = pointGateway.getBalance(payment.getMemberId());
        if (balance.compareTo(payment.getAmount()) < 0) {
            throw new InvalidPaymentException("Insufficient points");
        }
    }
}

// 3단계: 캡슐화 + Strategy 선택
@Service
public class PaymentService {
    @Autowired
    private Map<String, PaymentStrategy> paymentStrategies;
    
    @Autowired
    private PaymentTransactionRepository transactionRepo;
    
    // 전략 등록 (Spring DI로 자동화)
    @Configuration
    public static class PaymentStrategyConfig {
        @Bean
        public Map<String, PaymentStrategy> paymentStrategies(
            CardPaymentStrategy cardStrategy,
            PointPaymentStrategy pointStrategy,
            TransferPaymentStrategy transferStrategy) {
            
            Map<String, PaymentStrategy> strategies = new HashMap<>();
            strategies.put("CARD", cardStrategy);
            strategies.put("POINT", pointStrategy);
            strategies.put("TRANSFER", transferStrategy);
            return strategies;
        }
    }
    
    // 단순화된 메인 메서드
    public PaymentResult process(String paymentType, Payment payment) {
        PaymentStrategy strategy = getStrategy(paymentType);
        
        try {
            PaymentResult result = strategy.executePayment(payment);
            transactionRepo.save(PaymentTransaction.from(payment, result));
            return result;
        } catch (InvalidPaymentException e) {
            return PaymentResult.failure(e.getMessage());
        }
    }
    
    public RefundResult refund(String paymentType, String transactionId, BigDecimal amount) {
        PaymentStrategy strategy = getStrategy(paymentType);
        return strategy.refund(transactionId, amount);
    }
    
    public SettlementResult settle(String paymentType, LocalDate date) {
        PaymentStrategy strategy = getStrategy(paymentType);
        return strategy.settle(date);
    }
    
    private PaymentStrategy getStrategy(String paymentType) {
        PaymentStrategy strategy = paymentStrategies.get(paymentType);
        if (strategy == null) {
            throw new UnsupportedPaymentMethodException(
                "지원하지 않는 결제 수단: " + paymentType
            );
        }
        return strategy;
    }
}

// 4단계: 테스트 격리 (각 수단별 독립 테스트 가능)
@SpringBootTest
public class PointPaymentStrategyTest {
    @Mock
    private PointGateway pointGateway;
    
    @InjectMocks
    private PointPaymentStrategy strategy;
    
    @Test
    void testRefundWithInsufficientPoints() {
        // 포인트 환급 버그 재현 테스트
        String transactionId = "TXN-12345";
        
        given(pointGateway.refund(transactionId, BigDecimal.valueOf(10000)))
            .willReturn(PointRefundResult.success());
        
        RefundResult result = strategy.refund(transactionId, BigDecimal.valueOf(10000));
        
        assertThat(result.getStatus()).isEqualTo(RefundStatus.SUCCESS);
    }
}
```

### 3️⃣ 포인트 환급 버그 근본 원인 & 해결

**발생 원인:**
```java
// ❌ 기존 코드: 포인트 환급 로직에서 거래 기록 누락
else if ("POINT".equals(paymentType)) {
    // 포인트는 차감했는데...
    pointGateway.refund(transactionId, amount);
    // 환급 기록을 하지 않음! → 차감 기록만 남아 불일치 발생
}
```

**해결:**
```java
// ✅ Strategy 패턴으로 분리하면서 책임 명확화
@Override
public RefundResult refund(String transactionId, BigDecimal amount) {
    // 1. 환급 처리
    PointRefundResult result = pointGateway.refund(transactionId, amount);
    
    // 2. 반드시 장부에 기록 (Template Method로 강제 가능)
    ledgerRepo.save(PointLedger.ofRefund(transactionId, result));
    
    return result;
}
```

---

## 핵심 개념 정리

### 🔹 추상화 (Abstraction)
- **정의**: 결제의 공통 계약을 인터페이스/추상 클래스로 정의
- **효과**: `process()`, `refund()`, `settle()` 메서드 시그니처 통일
- **이점**: 새 결제 수단 추가 시 계약만 구현하면 됨

### 🔹 캡슐화 (Encapsulation)
- **정의

---

### 3. 🎤 면접 질문

# OOP 4대 원칙을 활용한 전자상거래 시스템 설계

## 모범 답안

### 1단계: 현재 문제 분석 및 OOP 원칙 적용

**문제점:**
- if-else 분기로 인한 **개방-폐쇄 원칙(OCP) 위반**
- 새로운 상품 타입 추가 시 기존 코드 수정 필요
- 테스트 커버리지 저하 및 버그 위험 증가

**해결책: 상속과 다형성 적용**

```java
// 1. 추상화: 공통 인터페이스/추상 클래스 정의
@Entity
@Inheritance(strategy = InheritanceType.JOINED)
@DiscriminatorColumn(name = "product_type")
public abstract class Product {
    
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;
    
    private String name;
    private String description;
    
    // 공통 필드만 정의
    
    // 추상 메서드: 각 상품 타입이 반드시 구현해야 함
    public abstract BigDecimal calculatePrice(OrderContext context);
    public abstract void processInventory(Long quantity);
    public abstract ShippingInfo getShippingInfo();
    
    // 템플릿 메서드 패턴으로 주문 프로세스 제어
    public final OrderResult processOrder(OrderContext context) {
        validateInventory(context.getQuantity());
        BigDecimal price = calculatePrice(context);
        processInventory(context.getQuantity());
        return createOrder(price, context);
    }
    
    private void validateInventory(Long quantity) {
        // 공통 검증 로직
    }
    
    private OrderResult createOrder(BigDecimal price, OrderContext context) {
        // 공통 주문 생성 로직
    }
}

// 2. 상속: 각 상품 타입별 구체적 구현
@Entity
public class PhysicalProduct extends Product {
    
    @Column(name = "weight")
    private BigDecimal weight;
    
    @Column(name = "dimensions")
    private String dimensions;
    
    @OneToMany(mappedBy = "product")
    private List<Warehouse> warehouses = new ArrayList<>();
    
    @Override
    public BigDecimal calculatePrice(OrderContext context) {
        // 실물상품: 기본가 + 배송비 + 무게기반 추가비
        BigDecimal basePrice = this.getPrice();
        BigDecimal shippingFee = calculateShippingFee();
        BigDecimal weightFee = weight.multiply(BigDecimal.valueOf(0.5));
        return basePrice.add(shippingFee).add(weightFee);
    }
    
    @Override
    public void processInventory(Long quantity) {
        // 실물상품: 창고에서 차감
        Warehouse warehouse = warehouses.stream()
            .filter(w -> w.getQuantity() >= quantity)
            .findFirst()
            .orElseThrow(() -> new InsufficientInventoryException());
        
        warehouse.decreaseQuantity(quantity);
    }
    
    @Override
    public ShippingInfo getShippingInfo() {
        return new PhysicalShippingInfo(
            this.weight,
            this.dimensions,
            ShippingMethod.PARCEL
        );
    }
    
    private BigDecimal calculateShippingFee() {
        // 무게 기반 배송비 계산
        if (weight.compareTo(BigDecimal.valueOf(5)) < 0) {
            return BigDecimal.valueOf(3000);
        }
        return BigDecimal.valueOf(5000);
    }
}

@Entity
public class EBook extends Product {
    
    @Column(name = "file_path")
    private String filePath;
    
    @Column(name = "file_size_mb")
    private Integer fileSizeMb;
    
    @Column(name = "download_count")
    private Integer downloadCount;
    
    @Override
    public BigDecimal calculatePrice(OrderContext context) {
        // 전자책: 기본가만 (배송비 없음, 할인 적용 가능)
        BigDecimal basePrice = this.getPrice();
        
        // 볼륨 할인 로직
        if (context.getQuantity() >= 10) {
            basePrice = basePrice.multiply(BigDecimal.valueOf(0.9));
        }
        return basePrice;
    }
    
    @Override
    public void processInventory(Long quantity) {
        // 전자책: 무제한 재고 (실제로는 라이선스 체크만 수행)
        if (!isLicenseValid()) {
            throw new InvalidLicenseException();
        }
        this.downloadCount += quantity;
    }
    
    @Override
    public ShippingInfo getShippingInfo() {
        return new DigitalShippingInfo(
            this.filePath,
            ShippingMethod.INSTANT_DOWNLOAD
        );
    }
    
    private boolean isLicenseValid() {
        // 라이선스 유효성 검증
        return true;
    }
}

@Entity
public class SubscriptionProduct extends Product {
    
    @Column(name = "billing_cycle_days")
    private Integer billingCycleDays;
    
    @Column(name = "auto_renewal")
    private Boolean autoRenewal;
    
    @OneToMany(mappedBy = "product")
    private List<Subscription> subscriptions = new ArrayList<>();
    
    @Override
    public BigDecimal calculatePrice(OrderContext context) {
        // 구독상품: 주기별 가격 계산
        BigDecimal monthlyPrice = this.getPrice();
        int months = context.getDurationMonths();
        
        // 장기 구독 할인
        BigDecimal discountRate = months >= 12 ? 
            BigDecimal.valueOf(0.85) : BigDecimal.ONE;
        
        return monthlyPrice
            .multiply(BigDecimal.valueOf(months))
            .multiply(discountRate);
    }
    
    @Override
    public void processInventory(Long quantity) {
        // 구독상품: 새로운 구독 생성
        for (int i = 0; i < quantity; i++) {
            Subscription subscription = new Subscription(
                this,
                LocalDateTime.now().plusDays(billingCycleDays)
            );
            subscriptions.add(subscription);
        }
    }
    
    @Override
    public ShippingInfo getShippingInfo() {
        return new SubscriptionShippingInfo(
            billingCycleDays,
            autoRenewal
        );
    }
}

// 3. 캡슐화: 클라이언트 코드는 Product 인터페이스만 사용
public class OrderService {
    
    private final ProductRepository productRepository;
    private final OrderRepository orderRepository;
    
    @Transactional
    public OrderResult createOrder(Long productId, OrderContext context) {
        // 실제 구현체를 알 필요 없음 (다형성)
        Product product = productRepository.findById(productId)
            .orElseThrow(() -> new ProductNotFoundException());
        
        // product가 실제로 어떤 타입인지 상관없이 동작
        OrderResult result = product.processOrder(context);
        
        orderRepository.save(result.getOrder());
        
        return result;
    }
}
```

---

### 2. JPA 상속 전략 선택: SINGLE_TABLE vs JOINED

**비교 분석:**

```java
// 전략 1: SINGLE_TABLE (단일 테이블)
@Entity
@Inheritance(strategy = InheritanceType.SINGLE_TABLE)
@DiscriminatorColumn(name = "product_type", discriminatorType = STRING)
public abstract class Product { ... }

/*
장점:
✅ 조회 성능 우수 (조인 불필요)
✅ 간단한 쿼리 생성
✅ 트랜잭션 처리 단순

단점:
❌ 모든 필드가 한 테이블에 (null 많음)
❌ 데이터 정규화 떨어짐
❌ 자식 특화 필드 제약 많음
❌ 테이블 크기 증가
*/

// 전략 2: JOINED (조인)
@Entity
@Inheritance(strategy = InheritanceType.JOINED)
@DiscriminatorColumn(name = "product_type")
public abstract class Product { ... }

@Entity
@PrimaryKeyJoinColumn(name = "product_id")
public class PhysicalProduct extends Product {
    @Column(name = "weight")
    private BigDecimal weight;
}

/*
장점:
✅ 데이터 정규화 (중복 최소화)
✅ 자식 특화 필드 자유로움
✅ null 값 최소화
✅ 테이블 명확한 분리
✅ 확장성 우수

단점:
❌ 조회 시 조인 발생
❌ 쿼리 복잡도 증가
❌ 성능

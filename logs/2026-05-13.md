# 2026-05-13 학습 기록

## 📌 토픽

JVM 메모리 구조 (Heap, Stack, Method Area)

## ❓ 질문 (3문제)

### 1. 🔧 문제 상황

당신이 만든 앱이 자꾸 "OutOfMemoryError"라는 에러로 튕깁니다. 개발팀에서는 "Heap이 부족하다"고 하는데, 동시에 다른 팀원은 "Stack 영역도 체크해봐야 한다"고 말합니다. 

메모리가 부족하다는 건 알겠는데, 왜 JVM은 메모리를 Heap, Stack, Method Area로 나눠서 관리하고 있을까요? 만약 메모리가 하나의 덩어리였다면 어떤 문제가 생겼을까요?

## 💬 나의 답변

• Stack / Heap / Method Area 로 나누는 이유는 *서로 다른 생명주기와 접근 패턴을 가진 데이터들을 분리해서, 각자에 맞는 관리·최적화를 하기 위해서*입니다.
• 만약 한 덩어리였다면,
• GC 부하증가,
• 성능 저하,
• 메모리 파편화,
• 디버깅·튜닝 난이도 상승
같은 문제가 생겨서 지금처럼 안정적이고 빠른 JVM을 구현하기 훨씬 어려웠을 겁니다.

## 🤖 AI 피드백

(피드백 없음)

## 📚 모범 답안 & 개념 정리

### 1. 🔧 문제 상황

# JVM 메모리 구조와 분리 설계의 필요성

## 모범 답안

**JVM이 메모리를 분리하는 이유는 "접근 패턴과 생명주기가 다르기 때문"입니다.**

### 1️⃣ 왜 분리되어 있는가?

```
┌─────────────────────────────────┐
│        JVM 메모리 구조          │
├─────────────────────────────────┤
│  Heap (동적 할당, GC 대상)      │
│  - 객체 인스턴스 저장           │
│  - 스레드 간 공유               │
│  - 생명주기: 불명확             │
├─────────────────────────────────┤
│  Stack (스레드마다 독립)        │
│  - 메서드 호출 스택             │
│  - 지역변수, 참조값             │
│  - 생명주기: 메서드 종료 시 자동 해제 │
├─────────────────────────────────┤
│  Method Area (클래스 로딩)      │
│  - 클래스 정보, 메서드 코드     │
│  - 상수 풀, 필드 정보           │
│  - 생명주기: 클래스 언로드 시까지│
└─────────────────────────────────┘
```

### 2️⃣ 분리되지 않았다면?

```java
// 만약 메모리가 하나였다면...
public class UnifiedMemoryProblem {
    
    // 문제 1: 메모리 접근 속도
    // Stack처럼 빠른 접근이 필요한 데이터와
    // Heap처럼 느린 GC 대상을 섞으면 성능 악화
    
    // 문제 2: 메모리 해제 전략
    // Stack: 후입선출(LIFO) - 자동 해제
    // Heap: 복잡한 참조 관계 - GC 필요
    // 이를 하나의 메커니즘으로 통일 불가능
    
    // 문제 3: 스레드 안전성
    // Stack: 스레드별 독립 → Race condition 없음
    // Heap: 스레드 간 공유 → 동기화 필요
    // 혼합 시 복잡도 폭증
}
```

### 3️⃣ 실제 OutOfMemoryError 분석

```java
public class OOMAnalysis {
    
    // Case 1: HeapOutOfMemory (가장 일반적)
    // 증상: 객체가 계속 생성되고 GC로도 해제 안 됨
    public void heapOOM() {
        List<byte[]> list = new ArrayList<>();
        while(true) {
            list.add(new byte[1024 * 1024]); // 1MB씩 계속 추가
            // → 메모리가 찬다 → GC 실행 → 참조가 남아있어서 해제 못함
        }
    }
    
    // Case 2: StackOverflowError (재귀 호출)
    // 증상: Stack 공간이 부족 (보통 JVM 설정으로 크기 고정)
    public void stackOOM() {
        stackOOM(); // 무한 재귀 → 스택 프레임 계속 쌓임
        // Stack은 동적 할당 불가 → 즉시 에러 발생
    }
    
    // Case 3: PermGen/Metaspace OOM (클래스 언로드 못함)
    // 증상: 너무 많은 클래스 로딩 또는 언로드 실패
    public void methodAreaOOM() {
        // 동적 클래스 생성 (프록시, CGLIB 등)
        for(int i = 0; i < 1000000; i++) {
            Class<?> clazz = new ClassLoader() {
                // 계속 새로운 클래스 로드
            }.loadClass("Dynamic" + i);
        }
    }
}
```

---

## 핵심 개념 정리

### 📌 **Heap (힙)**
- **용도**: 동적으로 생성되는 모든 객체 저장
- **특징**:
  - 스레드 간 공유
  - GC의 대상
  - 크기 동적 할당 가능
  - 접근 속도 상대적으로 느림
- **크기 조정**: `-Xms`, `-Xmx` (초기/최대 크기)
- **OOM 원인**: 메모리 누수, 과도한 객체 생성

### 📌 **Stack (스택)**
- **용도**: 메서드 호출, 지역변수, 참조값(주소) 저장
- **특징**:
  - 스레드마다 독립적
  - LIFO(후입선출) 방식
  - 자동 메모리 해제
  - 고정 크기 (동적 할당 불가)
  - 접근 속도 빠름
- **크기 조정**: `-Xss` (스레드별 스택 크기)
- **OOM 원인**: 재귀 호출 깊이, 많은 지역변수

### 📌 **Method Area (메서드 영역)**
- **용도**: 클래스 구조, 메서드 코드, 상수, 필드 정보 저장
- **특징**:
  - 모든 스레드 공유
  - 클래스 로딩 시 할당
  - 클래스 언로드 시 해제
  - Java 8+에서는 Metaspace (Native Memory)
- **OOM 원인**: 동적 클래스 생성, 클래스로더 메모리 누수

### 📌 **분리의 이점**

| 측면 | 효과 |
|------|------|
| **성능** | Stack은 빠른 접근, Heap은 GC로 최적화 |
| **메모리 안정성** | Stack 오버플로우가 Heap에 영향 없음 |
| **동시성** | 스레드별 Stack으로 동기화 비용 감소 |
| **생명주기 관리** | 각 영역에 맞는 해제 전략 적용 |

---

## 실무 포인트

### 🔍 **OutOfMemoryError 진단 프로세스**

```java
public class DiagnosticTips {
    
    // 1️⃣ 로그 확인
    // java.lang.OutOfMemoryError: Java heap space
    // → Heap이 부족 (객체 누수 의심)
    
    // java.lang.StackOverflowError
    // → Stack 부족 (재귀 호출 확인)
    
    // java.lang.OutOfMemoryError: Metaspace
    // → 클래스 언로드 못함 (ClassLoader 누수)
    
    // 2️⃣ 힙 덤프 분석
    // JVM 옵션: -XX:+HeapDumpOnOutOfMemoryError
    // -XX:HeapDumpPath=/path/to/dump
    public void analyzeHeapDump() {
        // Eclipse MAT, JProfiler 등으로 분석
        // → 어떤 객체가 메모리를 차지하는지 확인
        // → GC root 추적으로 왜 해제되지 않는지 파악
    }
    
    // 3️⃣ 메모리 설정 권장값
    String jvmOptions = """
        -Xms4g              # 초기 Heap 크기
        -Xmx8g              # 최대 Heap 크기
        -Xss512k            # 스레드당 Stack 크기
        -XX:+UseG1GC        # G1GC 사용 (Java 9+)
        -XX:MaxGCPauseMillis=200  # GC pause 제한
        """;
}
```

### 💡 **실무 체크리스트**

```java
public class PracticalChecklist {
    
    // ✅ Heap OOM 방지
    // 1. 불필요한 객체 참조 제거 (null 처리)
    public void preventMemoryLeak() {
        List<String> list = new ArrayList<>();
        list.add("data");
        // 사용 후
        list.clear();
        list = null; // 명시적 해제
    }
    
    // 2. 캐시 크기 제한
    public class LimitedCache {
        private final Map<String, String> cache =

# 2026-06-20 학습 기록

## 📌 토픽

JVM 메모리 구조 (Heap, Stack, Method Area)

## ❓ 질문 (3문제)

### 1. 🔧 문제 상황

**상황: 메모리 부족 문제**

당신의 서버 애플리케이션이 계속 실행되다가 어느 순간 갑자기 OutOfMemoryError로 죽어버립니다. 같은 코드가 로컬에서는 잘 돌아가는데 말이죠.

에러 메시지를 보니 "Java Heap space"라고 되어 있습니다. 하지만 당신 팀의 선임 개발자는 "Stack이 터진 것 같은데?"라고 말합니다.

두 가지를 구분하기 위해, 당신은 먼저 다음을 생각해야 합니다:

**Heap과 Stack은 어떤 상황에서 각각 터지고, 왜 그 차이가 생기는지 간단히 설명할 수 있을까요? (예: 무한 반복문 vs 대량의 객체 생성)**

## 💬 나의 답변

(답변 없음)

## 🤖 AI 피드백

(피드백 없음)

## 📚 모범 답안 & 개념 정리

### 1. 🔧 문제 상황

# JVM 메모리 구조 - Heap vs Stack OutOfMemoryError

## 모범 답안

**Java Heap space 에러는 객체 생성 과다로 발생하고, Stack 에러는 호출 깊이 과다로 발생합니다.**

### Heap이 터지는 경우 (Java Heap space)
```java
// ❌ 대량 객체 생성 - Heap 고갈
List<byte[]> list = new ArrayList<>();
while (true) {
    list.add(new byte[1024 * 1024]); // 1MB씩 계속 생성
    // GC가 이들을 회수할 수 없음 (list가 계속 참조)
}

// ❌ 메모리 누수
public class MemoryLeak {
    private static List<Object> cache = new ArrayList<>();
    
    public void processData(Object data) {
        cache.add(data); // 영구적으로 쌓임
    }
}
```

**원인:**
- 생성된 객체가 GC 대상이 되지 않음 (강한 참조 유지)
- 시간이 지날수록 메모리 사용량 증가
- 로컬과 다른 이유: **데이터량 차이** (개발DB vs 운영DB)

---

### Stack이 터지는 경우 (StackOverflowError)
```java
// ❌ 무한 재귀
public int fibonacci(int n) {
    return fibonacci(n - 1) + fibonacci(n - 2); // 스택 깊이 무한 증가
}

// ❌ 순환 참조 직렬화
@Data
public class Node {
    private Node parent;
    private Node child;
}
// parent <-> child 순환 참조 + 깊은 직렬화 시도
```

**원인:**
- 함수 호출이 반환되지 않고 계속 쌓임
- 각 호출마다 Stack frame 생성
- 로컬에서 안 터지는 이유: **테스트 케이스의 입력값이 작음**

---

## 핵심 개념 정리

### 📍 Heap 메모리
| 항목 | 특성 |
|------|------|
| **용도** | 모든 객체(Object) 인스턴스 저장 |
| **생명주기** | GC에 의해 자동 회수 |
| **공유** | 모든 스레드가 공유 |
| **크기** | `-Xmx` 옵션으로 제한 |
| **에러 시점** | 즉시 아님 (GC 후에도 부족할 때) |

### 📍 Stack 메모리
| 항목 | 특성 |
|------|------|
| **용도** | 메서드 호출, 지역변수, 참조값 |
| **생명주기** | 메서드 반환 시 자동 해제 |
| **독립성** | **각 스레드마다 독립적** |
| **크기** | `-Xss` 옵션으로 제한 (보통 1MB) |
| **에러 시점** | 호출 깊이가 깊어질수록 |

### 📍 Method Area (Class Area)
- 클래스 정의, 메서드 바이트코드, 상수풀 저장
- 문제 상황에서는 거의 관련 없음

---

## 실무 포인트

### 🔍 상황 분석 체크리스트

```java
// 1️⃣ Heap 문제 진단
public void diagnoseHeapIssue() {
    // - GC 로그 분석: Full GC 빈도 증가?
    // - Heap 덤프 분석: 어떤 객체가 많이 남아있나?
    // - 메모리 누수: 캐시, 컬렉션, 리스너 미제거?
}

// 2️⃣ Stack 문제 진단
public void diagnoseStackIssue() {
    // - 스택 트레이스: 같은 메서드 반복?
    // - 재귀 깊이: 입력 크기에 따라 증가?
    // - 직렬화: 깊은 객체 구조?
}
```

### ⚠️ 로컬 vs 프로덕션 차이 원인

```
┌─────────────────────────────────────────────┐
│  로컬                                        │
├─────────────────────────────────────────────┤
│ ✓ 작은 테스트 데이터 (몇 개 행)              │
│ ✓ -Xmx 크기 (보통 2GB 이상)                │
│ ✓ 제한된 스레드 (몇 개)                     │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│  프로덕션                                    │
├─────────────────────────────────────────────┤
│ ✗ 대량의 실제 데이터 (100만 행 이상)        │
│ ✗ 제한된 -Xmx (보통 1GB 이상 4GB 이하)     │
│ ✗ 많은 동시 요청 (1000+ 스레드)             │
│ ✗ 메모리 누수 (시간이 지날수록 악화)        │
└─────────────────────────────────────────────┘
```

### 🛠️ 실무 해결 방법

#### Heap 문제 해결
```java
// 1. 메모리 누수 제거
@Component
public class DataCache {
    // ❌ 나쁜 예
    // private static List<Data> cache = new ArrayList<>();
    
    // ✅ 좋은 예
    private final Cache<String, Data> cache = 
        CacheBuilder.newBuilder()
            .expireAfterWrite(10, TimeUnit.MINUTES)
            .maximumSize(10000)
            .build();
}

// 2. 배치 처리로 메모리 분산
public void processBigData() {
    try (Stream<Data> stream = dataRepository.findAll()) {
        stream.batch(1000) // 1000개씩 처리
              .forEach(this::process);
    }
}

// 3. Weak/Soft Reference 활용
Map<String, SoftReference<ExpensiveObject>> cache = 
    new HashMap<>();
```

#### Stack 문제 해결
```java
// 1. 재귀 → 반복문
// ❌ 재귀
public int factorial(int n) {
    return n <= 1 ? 1 : n * factorial(n - 1);
}

// ✅ 반복
public int factorial(int n) {
    int result = 1;
    for (int i = 2; i <= n; i++) {
        result *= i;
    }
    return result;
}

// 2. 깊은 객체 구조 평탄화
// ❌ 순환 구조
@JsonIgnoreProperties({"parent", "children"})
public class TreeNode {
    private TreeNode parent;
    private List<TreeNode> children;
}

// 3. Stack 크기 조정 (임시 해결책)
// JVM 옵션: -Xss 2m (기본값 1m)
```

### 📊 모니터링 명령어

```bash
# Heap 덤프 생성
jmap -dump:live,format=b,file=heap.bin <PID>

# Heap 상태 확인
jstat -gc -h20 <PID> 1000

# 스택 트레이스 확인
jstack <PID> | grep -A 10 "RUNNABLE"

# 메모리 사용량 모니터링
jconsole <PID>
```

---

## 최종 결론

| 항목 | Heap | Stack |
|------|------|-------|
| **원인** | 객체 생성 > GC 속도 | 호출 깊이 > Stack 크기 |
| **에러** | `OutOfMemoryError: Java Heap space` | `StackOverflowError` |
| **디버깅** | 힙 덤프, GC 로그 | 스택 트레이스 |
| **해결** | 메모리 누수 제거, 캐시 정책 | 재귀 제거, 구조 변경 |

**선임 개발자의 "Stack 터진 것 같은데?"는 틀렸습니다.** 에러 메시지가 명확하게 "Java Heap space"라면 → **객체 생성 과다 또는

# 2026-05-25 학습 기록

## 📌 토픽

상속과 다형성

## ❓ 질문 (3문제)

### 1. 🎤 면접 질문

**메인 질문:**

여러분이 학교 다닐 때를 생각해보세요. 선생님, 학생, 교감선생님은 모두 다른 역할을 하지만, 모두 "학교 구성원"이라고 부를 수 있어요. 프로그래밍에서도 이렇게 다양한 것들을 하나의 이름으로 묶고 싶은 경우가 있습니다. 상속과 다형성이 이런 문제를 어떻게 해결한다고 생각하나요? 직접 겪었던 상황이나 상상해본 코드 상황을 예로 들어서 설명해주세요.

**꼬리 질문:**

만약 상속과 다형성 없이 선생님, 학생, 교감선생님을 따로 취급해야 한다면, 프로그램을 관리하는 것이 어떻게 불편할까요?

## 💬 나의 답변

(답변 없음)

## 🤖 AI 피드백

(피드백 없음)

## 📚 모범 답안 & 개념 정리

### 1. 🎤 면접 질문

# 상속과 다형성 - 면접 모범 답변

## 모범 답안

좋은 질문입니다. 상속과 다형성이 해결하는 핵심 문제를 설명하겠습니다.

### 상속과 다형성의 역할

상속과 다형성은 **공통된 특성을 추상화하고, 다양한 구현을 통일된 인터페이스로 다루는** 문제를 해결합니다.

```java
// 1단계: 공통의 추상화 - 상속
public abstract class SchoolMember {
    protected String name;
    protected String id;
    
    public abstract void work();
    public abstract String getRole();
}

public class Teacher extends SchoolMember {
    @Override
    public void work() {
        System.out.println(name + "이(가) 수업을 진행합니다");
    }
    
    @Override
    public String getRole() {
        return "선생님";
    }
}

public class Student extends SchoolMember {
    @Override
    public void work() {
        System.out.println(name + "이(가) 수업을 듣습니다");
    }
    
    @Override
    public String getRole() {
        return "학생";
    }
}

public class Principal extends SchoolMember {
    @Override
    public void work() {
        System.out.println(name + "이(가) 학교를 관리합니다");
    }
    
    @Override
    public String getRole() {
        return "교감선생님";
    }
}

// 2단계: 다형성으로 통일된 처리
public class SchoolSystem {
    private List<SchoolMember> members = new ArrayList<>();
    
    public void addMember(SchoolMember member) {
        members.add(member);
    }
    
    // 모든 구성원을 동일하게 처리
    public void startDay() {
        for (SchoolMember member : members) {
            member.work();  // 다형성: 실제 객체 타입에 따라 다른 동작
            System.out.println("역할: " + member.getRole());
        }
    }
}

// 사용
SchoolSystem school = new SchoolSystem();
school.addMember(new Teacher());
school.addMember(new Student());
school.addMember(new Principal());
school.startDay();  // 각각 다른 work() 구현이 실행됨
```

### 상속과 다형성 없을 경우의 문제점

```java
// 😱 상속 없이 모두 따로 취급하는 경우
public class TeacherManager {
    public void doTeacherWork(Teacher teacher) {
        System.out.println(teacher.getName() + "이(가) 수업을 진행합니다");
    }
}

public class StudentManager {
    public void doStudentWork(Student student) {
        System.out.println(student.getName() + "이(가) 수업을 듣습니다");
    }
}

public class PrincipalManager {
    public void doPrincipalWork(Principal principal) {
        System.out.println(principal.getName() + "이(가) 학교를 관리합니다");
    }
}

// 사용 - 끔찍함!
TeacherManager teacherMgr = new TeacherManager();
StudentManager studentMgr = new StudentManager();
PrincipalManager principalMgr = new PrincipalManager();

teacherMgr.doTeacherWork(teacher);
studentMgr.doStudentWork(student);
principalMgr.doPrincipalWork(principal);

// 새로운 역할이 추가되면? 새로운 Manager 클래스 필요!
// 코드 중복 발생, 유지보수 악몽
```

---

## 핵심 개념 정리

### 상속 (Inheritance)
- **정의**: 부모 클래스의 특성(메서드, 필드)을 자식 클래스가 물려받는 메커니즘
- **목적**: 공통된 특성을 한 곳에서 관리
- **IS-A 관계**: Student IS-A SchoolMember (학생은 학교 구성원이다)

### 다형성 (Polymorphism)
- **정의**: 같은 메서드 호출이 객체의 실제 타입에 따라 다르게 동작
- **종류**:
  - **메서드 오버라이딩**: 부모 메서드를 자식이 재구현
  - **메서드 오버로딩**: 같은 이름, 다른 파라미터
- **목적**: 유연하고 확장 가능한 코드

### 상속의 핵심 이점
| 문제 | 해결책 |
|------|--------|
| 코드 중복 | 공통 로직을 부모 클래스에 작성 |
| 유지보수 어려움 | 부모 클래스만 수정하면 모든 자식에 반영 |
| 새로운 타입 추가 시 기존 코드 수정 필요 | 새 자식 클래스만 추가하면 됨 |
| 다양한 타입을 개별적으로 처리 | 부모 타입으로 통일하여 처리 |

### 다형성의 핵심 이점
```
"동일한 인터페이스로 다양한 객체를 다룬다"
= 코드가 확장에 열려있고 수정에 닫혀있다 (OCP 원칙)
```

---

## 실무 포인트

### 1️⃣ 실제 프로젝트 예시: 결제 시스템

```java
// 결제 방식이 계속 추가되는 현실
public abstract class PaymentMethod {
    public abstract void pay(double amount);
    public abstract String getMethodName();
}

public class CreditCardPayment extends PaymentMethod {
    @Override
    public void pay(double amount) {
        // 신용카드 결제 로직
        System.out.println(amount + "원을 신용카드로 결제");
    }
    
    @Override
    public String getMethodName() {
        return "신용카드";
    }
}

public class KakaoPayPayment extends PaymentMethod {
    @Override
    public void pay(double amount) {
        // 카카오페이 결제 로직
        System.out.println(amount + "원을 카카오페이로 결제");
    }
    
    @Override
    public String getMethodName() {
        return "카카오페이";
    }
}

// 결제 시스템 - 새로운 결제 방식 추가해도 수정 필요 없음!
public class PaymentProcessor {
    public void processPayment(PaymentMethod method, double amount) {
        method.pay(amount);  // 다형성
        // 결제 로그, DB 저장 등 공통 로직
    }
}
```

### 2️⃣ 반드시 피해야 할 패턴

```java
// ❌ 나쁜 예: instanceof 체크가 많은 코드
public void processPayment(Object payment, double amount) {
    if (payment instanceof CreditCard) {
        ((CreditCard)payment).chargeCard(amount);
    } else if (payment instanceof KakaoPay) {
        ((KakaoPay)payment).chargeKakaoPay(amount);
    } else if (payment instanceof ApplePay) {
        ((ApplePay)payment).chargeApple(amount);
    }
    // 새로운 결제 방식 추가될 때마다 else if 추가... 악몽!
}
```

### 3️⃣ 주의사항

```java
// ⚠️ 과도한 상속 계층 피하기
// 좋음: 3단계 정도
SchoolMember > Teacher > HighSchoolTeacher

// 나쁨: 너무 깊은 상속 계층
SchoolMember > Person > Teacher > HighSchoolTeacher > MathTeacher

// 💡 대안: 조합(Composition) 활용
public class Teacher {
    private Subject subject;  // 교과목을 필드로 갖기
    private Grade grade;      // 학년을 필드로 갖기
}
```

### 4️⃣ 면접관이 원하는 추가 답변 포인트

```
Q: "그렇다면 상속을 어디까지 써야 할까요?"
A: "단일 책임 원칙을 따릅니다. 
   - 공통 특성이 명확할 때만 상속
   - 3단계 이상 상속이 필요하면 조합(Composition) 고려
   - 'IS-A' 관계가 명확

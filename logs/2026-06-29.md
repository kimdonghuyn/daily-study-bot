# 2026-06-29 학습 기록

## 📌 토픽

캡슐화와 접근 제어자

## ❓ 질문 (3문제)

### 1. 🎤 면접 질문

캡슐화와 접근 제어자에 대해 물어보겠습니다.

우리가 사용하는 TV를 생각해봅시다. TV의 리모컨으로는 채널을 돌리고 볼륨을 조절할 수 있지만, TV 내부의 복잡한 회로나 부품을 직접 건드릴 수는 없습니다. 만약 누구나 내부를 마음대로 만질 수 있다면 TV가 고장 날 가능성이 높겠죠.

프로그래밍에서 말하는 "캡슐화와 접근 제어자"도 비슷한 개념입니다. 

당신이 팀 프로젝트에서 "사용자 잔액"을 관리하는 기능을 만들었다고 가정해봅시다. 다른 팀원이 코드를 보더니 직접 사용자의 잔액 값을 마음대로 수정해버렸습니다. 결과적으로 잔액이 음수가 되거나 부정확해졌어요.

이런 상황을 방지하기 위해 "누가 무엇에 접근할 수 있는지"를 제한하는 것이 바로 접근 제어자입니다.

**당신이라면, 이런 문제를 해결하기 위해 구체적으로 어떤 방식으로 제한을 두고 싶나요? 예를 들어 "사용자 잔액"에 대해서는 누구에게 어떤 권한까지만 주고 싶은지 말씀해주세요.**

그리고 꼬리 질문입니다.

**만약 잔액을 완전히 숨긴다면, 다른 팀원이 잔액을 알아야 할 때는 어떻게 해야 할까요?**

## 💬 나의 답변

1. 잔액은 우선 private로 두고 직접적으로 수정이 불가능하게 하고, 해당 잔액을 조회 할땐 getter 메서드를 public 으로 두고 조회를 하도록 하며 만약 수정을 한다면 수정 메서드를 public 으로 두되 정확한 검증을 통과해야지만 되도록 할 것 같다.
2. 수정 권한을 제한하는거지 조회 권한을 제한하는게 아니라서 그냥 getter 메서드를 통해서 조회하면 된다.

## 🤖 AI 피드백

✅ 잘한 점 (구체적으로)
- 캡슐화의 핵심 개념을 정확히 이해하고 있습니다. private으로 필드를 보호하고, public 메서드로 제어하는 기본 원칙을 올바르게 설명했습니다.
- getter/setter 패턴의 필요성과 검증 로직의 중요성을 인식하고 있습니다. "정확한 검증을 통과해야지만"이라는 표현에서 비즈니스 로직 보호 의식이 보입니다.
- 꼬리 질문에 대해 조회와 수정 권한을 구분하는 논리적 사고를 보여주었습니다.

⚠️ 보완할 점 (구체적으로)
- 구체적인 검증 내용이 부재합니다. "정확한 검증"이 무엇인지 예시가 필요합니다. 예를 들어, "잔액은 0 이상이어야 한다", "거래액이 현재 잔액을 초과할 수 없다", "트랜잭션 기록이 남아야 한다" 등을 언급했다면 더 깊이 있었을 것입니다.
- 접근 제어자의 세분화가 부족합니다. private/public만 언급했는데, protected나 package-private(default) 같은 다른 수준의 접근 제어와 실무에서의 사용 사례를 언급했으면 좋았습니다.
- 불변성(Immutability) 관점이 없습니다. getter에서 원본 객체를 그대로 반환하면 안 되는 경우들(예: List, Date 같은 mutable 객체)에 대한 고려가 부족합니다.

💡 핵심 포인트 (놓친 부분)
- 실무 수준의 답변이 되려면: 검증 예시 + 트랜잭션 로깅 + 예외 처리 + 동시성 고려(synchronized, volatile) 같은 추가 요소를 언급했으면 시니어 개발자답습니다.
- 예제 코드 제시: 구두 설명보다 간단한 코드(withdraw/deposit 메서드에서의 검증 로직)를 보여주면 훨씬 설득력이 있습니다.
- 비즈니스 영향: "잔액이 음수가 되는 것"뿐만 아니라 "금융 규정 준수", "감사 추적", "동시 거래 처리"까지 언급했으면 시니어 관점입니다.

## 📚 모범 답안 & 개념 정리

### 1. 🎤 면접 질문

# 캡슐화와 접근 제어자 - 모범 답안

## 모범 답안

### 1단계: 문제 상황 인식
먼저 이 문제의 핵심은 **데이터 무결성 보호**입니다. 사용자 잔액은 비즈니스 로직상 중요한 데이터이므로, 임의의 변경을 방지해야 합니다.

### 2단계: 구체적 해결 방안

```java
// ❌ 잘못된 예: 데이터가 노출됨
public class User {
    public double balance;  // 누구나 직접 접근 가능
}

// ✅ 올바른 예: 캡슐화를 통한 접근 제어
public class User {
    // 1. private으로 데이터 보호
    private double balance;
    
    // 2. 읽기만 허용 (getter)
    public double getBalance() {
        return balance;
    }
    
    // 3. 검증 로직이 포함된 쓰기 (setter)
    public void deposit(double amount) {
        if (amount <= 0) {
            throw new IllegalArgumentException("입금액은 0보다 커야 합니다");
        }
        this.balance += amount;
    }
    
    public boolean withdraw(double amount) {
        if (amount <= 0) {
            throw new IllegalArgumentException("출금액은 0보다 커야 합니다");
        }
        if (amount > this.balance) {
            throw new InsufficientBalanceException("잔액이 부족합니다");
        }
        this.balance -= amount;
        return true;
    }
}
```

### 3단계: 접근 제어자 설정 전략

| 접근자 | 대상 | 이유 |
|--------|------|------|
| **private** | `balance` (필드) | 직접 접근/수정 금지 |
| **public** | `getBalance()` | 조회 목적으로 누구나 조회 가능 |
| **public** | `deposit()`, `withdraw()` | 비즈니스 로직을 통한 통제된 수정 |

### 4단계: 꼬리 질문 답변

> "잔액을 완전히 숨기면 다른 팀원이 잔액을 알 수 없지 않나?"

**답변:**
```java
// 해결 방법: getter 메서드를 통한 제어된 접근

// ✅ 방법 1: 단순 조회 (읽기 전용)
public double getBalance() {
    return balance;
}

// ✅ 방법 2: 감사(Audit) 목적으로 로깅 추가
public double getBalance() {
    auditLog.record("잔액 조회: " + LocalDateTime.now());
    return balance;
}

// ✅ 방법 3: 특정 권한이 있는 사용자만 조회 허용
public double getBalance(User requester) {
    if (!requester.hasPermission("BALANCE_VIEW")) {
        throw new AccessDeniedException("잔액 조회 권한이 없습니다");
    }
    return balance;
}

// ✅ 방법 4: 민감한 정보는 마스킹
public String getBalanceFormatted() {
    return String.format("***.%02d", (int)(balance % 100));
}
```

---

## 핵심 개념 정리

### 📌 캡슐화 (Encapsulation)
- **정의**: 객체의 내부 상태와 구현 세부사항을 숨기고, 필요한 기능만 노출
- **목적**: 데이터 무결성 보호, 외부 변경 영향 최소화, 유지보수성 향상
- **원칙**: "구현은 숨기고, 인터페이스만 공개"

### 📌 접근 제어자 4가지

```
┌─────────────┬──────────┬──────────┬──────────┬──────────┐
│ 제어자      │ 같은 클래스 │ 같은 패키지 │ 상속 클래스 │ 전체 공개 │
├─────────────┼──────────┼──────────┼──────────┼──────────┤
│ private     │    ✅    │    ✗     │    ✗     │    ✗     │
│ (default)   │    ✅    │    ✅    │    ✗     │    ✗     │
│ protected   │    ✅    │    ✅    │    ✅    │    ✗     │
│ public      │    ✅    │    ✅    │    ✅    │    ✅    │
└─────────────┴──────────┴──────────┴──────────┴──────────┘
```

### 📌 Getter/Setter의 역할
- **Getter**: 외부에서 데이터 읽기 (읽기 권한 제어)
- **Setter**: 외부에서 데이터 수정 (검증 로직, 쓰기 권한 제어)
- **특징**: 메서드이므로 추가 로직 삽입 가능

### 📌 불변성 (Immutability)
```java
// 생성 후 변경 불가능하게 설계
public final class ImmutableUser {
    private final double balance;
    private final String name;
    
    public ImmutableUser(String name, double balance) {
        this.name = name;
        this.balance = balance;
    }
    
    // getter만 제공, setter 없음
}
```

---

## 실무 포인트

### 🏢 실제 현장 적용

#### 1️⃣ 계층별 접근 제어
```java
// DAO 계층
public class UserRepository {
    public User getUserById(Long id) {
        // private 메서드로 내부 로직 숨김
    }
    private User mapResultSet(ResultSet rs) {
        // 매핑 로직은 외부에 노출 X
    }
}

// Service 계층
public class UserService {
    private UserRepository userRepository;
    
    public UserBalance getUserBalance(Long userId) {
        User user = userRepository.getUserById(userId);
        return user.getBalance();  // getter 사용
    }
}

// Controller 계층
@RestController
public class UserController {
    public ResponseEntity<BalanceResponse> getBalance(@PathVariable Long userId) {
        // service 호출
    }
}
```

#### 2️⃣ 도메인 모델에서의 활용
```java
@Entity
public class Order {
    private Long id;
    private List<OrderItem> items;  // private
    private Money totalAmount;       // private
    
    // 주문항목 추가 시 총액 자동 재계산
    public void addItem(OrderItem item) {
        this.items.add(item);
        this.totalAmount = calculateTotal();
    }
    
    // 읽기만 제공
    public Money getTotalAmount() {
        return totalAmount;
    }
}
```

#### 3️⃣ 민감 정보 보호
```java
// ❌ 위험: 민감 정보 노출
public class PaymentCard {
    public String cardNumber;  // "1234-5678-9012-3456" 노출!
}

// ✅ 안전: 민감 정보 보호
public class PaymentCard {
    private String cardNumber;
    
    public String getCardNumberMasked() {
        return "****-****-****-" + cardNumber.substring(12);
    }
}
```

#### 4️⃣ 팀 협업 시 계약 명확화
```java
/**
 * @author 결제팀
 * @apiNote 잔액은 withdraw(), deposit()을 통해서만 수정 가능
 * @deprecated 직접 balance 필드 수정 금지!
 */
public class Wallet {
    private double balance;
    
    public double getBalance() { return balance; }
    public void withdraw(double amount) { /* ... */ }
    public void deposit(double amount) { /* ... */ }
}
```

### ⚠️ 주의사항

```java
// ❌ getter/setter를 무조건 만드는 것은 캡슐화가 아님
public class BadExample {
    private double balance;
    public double getBalance() { return balance; }
    public void setBalance(double balance) {  // 이건 캡슐화 아님!
        this.balance = balance;  // 검증 없이 모든 값 허용
    }
}

// ✅ 의도에 맞게 메서드 설계
public class GoodExample {
    private double balance;
    public double getBalance() { return balance;

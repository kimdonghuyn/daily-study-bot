# 2026-05-30 학습 기록

## 📌 토픽

SOLID — SRP와 OCP

## ❓ 질문 (3문제)

### 1. 🔧 문제 상황

당신은 '배달 앱'의 주문 처리 시스템을 만들고 있습니다. 현재 하나의 클래스가 주문 저장, 결제 처리, 배달 추적, 고객 알림까지 모든 걸 담당하고 있어서, 새로운 기능을 추가할 때마다 이 클래스를 수정해야 합니다.

예를 들어 "카카오페이 결제 추가", "SMS 알림 추가", "배달 파트너 시스템 변경" 같은 작은 수정 하나도 전체 주문 시스템이 깨질 위험이 있습니다.

**당신은 이 상황에서 어떤 책임들을 어떻게 나누어서 이 문제를 해결하겠고, 그렇게 하면 향후 새로운 결제 수단이나 알림 방식을 추가할 때 기존 코드를 건드리지 않고도 확장할 수 있을까요?**

## 💬 나의 답변

(답변 없음)

## 🤖 AI 피드백

(피드백 없음)

## 📚 모범 답안 & 개념 정리

### 1. 🔧 문제 상황

# SOLID 원칙 적용: SRP와 OCP를 통한 주문 시스템 리팩토링

## 모범 답안

### 문제 분석
현재 상황은 **단일 책임 원칙(SRP) 위반**으로, 하나의 `Order` 클래스가 5가지 이상의 변경 이유를 가지고 있습니다:
- 주문 저장 로직 변경
- 결제 방식 추가
- 배달 추적 시스템 변경
- 고객 알림 채널 추가
- 비즈니스 규칙 변경

### 해결 방식: 책임 분리 + 전략 패턴

```python
# ❌ 리팩토링 전: 모든 책임이 한 곳에
class OrderService:
    def process_order(self, order):
        # 1. 주문 저장
        self.save_order(order)
        
        # 2. 결제 처리 (직접 구현)
        if payment_method == "credit_card":
            self.process_credit_card(order)
        elif payment_method == "kakao_pay":
            self.process_kakao_pay(order)  # 새로운 기능 추가마다 수정 필요!
        
        # 3. 배달 추적
        self.start_delivery_tracking(order)
        
        # 4. 고객 알림 (직접 구현)
        self.send_email(order.customer_email)
        self.send_sms(order.customer_phone)  # 새로운 알림 추가마다 수정!
```

```python
# ✅ 리팩토링 후: SRP + OCP 적용

# 1단계: 책임 분리 - 각 기능을 독립적인 인터페이스로
from abc import ABC, abstractmethod

# 결제 책임
class PaymentProcessor(ABC):
    @abstractmethod
    def pay(self, order: Order) -> PaymentResult:
        pass

class CreditCardPaymentProcessor(PaymentProcessor):
    def pay(self, order: Order) -> PaymentResult:
        # 신용카드 결제 로직
        return PaymentResult(success=True)

class KakaoPayProcessor(PaymentProcessor):
    def pay(self, order: Order) -> PaymentResult:
        # 카카오페이 결제 로직
        return PaymentResult(success=True)

class NaverPayProcessor(PaymentProcessor):
    def pay(self, order: Order) -> PaymentResult:
        # 네이버페이 결제 로직 (새로운 기능 추가!)
        return PaymentResult(success=True)


# 알림 책임
class NotificationService(ABC):
    @abstractmethod
    def notify(self, customer: Customer, message: str) -> bool:
        pass

class EmailNotificationService(NotificationService):
    def notify(self, customer: Customer, message: str) -> bool:
        # 이메일 발송
        return True

class SMSNotificationService(NotificationService):
    def notify(self, customer: Customer, message: str) -> bool:
        # SMS 발송
        return True

class KakaoTalkNotificationService(NotificationService):
    def notify(self, customer: Customer, message: str) -> bool:
        # 카카오톡 발송 (새로운 기능 추가!)
        return True


# 배달 추적 책임
class DeliveryTracker(ABC):
    @abstractmethod
    def track(self, order: Order) -> None:
        pass

class BasicDeliveryTracker(DeliveryTracker):
    def track(self, order: Order) -> None:
        # 기본 배달 추적
        pass

class RealtimeGPSTracker(DeliveryTracker):
    def track(self, order: Order) -> None:
        # 실시간 GPS 추적 (새로운 방식 추가!)
        pass


# 주문 저장 책임
class OrderRepository(ABC):
    @abstractmethod
    def save(self, order: Order) -> None:
        pass

class DatabaseOrderRepository(OrderRepository):
    def save(self, order: Order) -> None:
        # DB 저장
        pass


# 2단계: 핵심 비즈니스 로직은 간단하게 - Orchestrator 패턴
class OrderService:
    """
    SRP: 오직 주문 처리의 흐름 조정만 담당
    의존성은 모두 주입받음 (DI)
    """
    def __init__(
        self,
        payment_processor: PaymentProcessor,
        notifiers: List[NotificationService],
        delivery_tracker: DeliveryTracker,
        order_repository: OrderRepository
    ):
        self.payment_processor = payment_processor
        self.notifiers = notifiers
        self.delivery_tracker = delivery_tracker
        self.order_repository = order_repository

    def process_order(self, order: Order) -> bool:
        try:
            # 1. 주문 저장
            self.order_repository.save(order)
            
            # 2. 결제 처리 (구체적인 결제 방식을 모름)
            payment_result = self.payment_processor.pay(order)
            if not payment_result.success:
                self._notify_all(f"결제 실패: {payment_result.error}")
                return False
            
            # 3. 배달 시작
            self.delivery_tracker.track(order)
            
            # 4. 고객 알림 (모든 채널에 동시에 발송)
            self._notify_all(f"주문 {order.id} 접수되었습니다")
            
            return True
        except Exception as e:
            self._notify_all(f"주문 처리 중 오류 발생: {str(e)}")
            return False

    def _notify_all(self, message: str) -> None:
        """모든 알림 채널에 동시 전송"""
        for notifier in self.notifiers:
            try:
                notifier.notify(self.order.customer, message)
            except Exception as e:
                # 하나의 알림 실패가 전체 흐름에 영향 없음
                logger.error(f"Notification failed: {str(e)}")
```

### 사용 예시 (Dependency Injection)

```python
# 의존성 설정 - 생성 시점에만 수정 필요
def create_order_service(config: Config) -> OrderService:
    # 결제 수단 선택
    payment_processor = {
        'credit_card': CreditCardPaymentProcessor(),
        'kakao_pay': KakaoPayProcessor(),
        'naver_pay': NaverPayProcessor(),
    }[config.payment_method]
    
    # 알림 채널 선택 (여러 개 조합 가능)
    notifiers = [
        EmailNotificationService(),
        SMSNotificationService(),
    ]
    if config.enable_kakao_talk:
        notifiers.append(KakaoTalkNotificationService())  # 기존 코드 수정 없음!
    
    # 배달 추적 방식 선택
    delivery_tracker = (
        RealtimeGPSTracker() 
        if config.use_gps 
        else BasicDeliveryTracker()
    )
    
    return OrderService(
        payment_processor=payment_processor,
        notifiers=notifiers,
        delivery_tracker=delivery_tracker,
        order_repository=DatabaseOrderRepository()
    )

# 실행
service = create_order_service(config)
service.process_order(order)
```

### 새로운 기능 추가 시 (OCP)

```python
# 새로운 결제 수단 추가 - 기존 코드 0개 수정!
class TossPaymentProcessor(PaymentProcessor):
    def pay(self, order: Order) -> PaymentResult:
        # Toss API 호출
        return PaymentResult(success=True)

# 설정만 변경
config.payment_method = 'toss_pay'
# 또는
payment_processor = TossPaymentProcessor()

# 기존 OrderService 코드는 건드리지 않음 ✅
```

---

## 핵심 개념 정리

### 🎯 SRP (Single Responsibility Principle)
- **정의**: 하나의 클래스는 **하나의 변경 이유**만 가져야 함
- **현재 문제**: OrderService는 5가지 변경 이유 보유
  - 결제 방식 변경
  - 알림 채널 추가
  - 배달 시스템 변경
  - 저장소 변경
  - 주문 규칙 변경
- **해결**: 각 책임을 **별도 인터페이스**로 분리

### 🔓 OCP (Open-Closed Principle)
- **정의**: 확장에는 열려있고, 수정에는 닫혀있어야 함
- **핵심 메커니즘**:
  ```
  추상화(Interface) → 구현체
